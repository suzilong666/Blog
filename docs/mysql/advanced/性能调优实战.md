# MySQL 性能调优实战

## 一、性能调优的方法论

### 1.1 调优的核心思路

性能调优是一个系统工程，需要遵循科学的方法：

```
┌─────────────────────────────────────────────────────────────┐
│                    性能调优方法论                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 发现问题    →  监控告警 / 用户反馈 / 性能测试            │
│       │                                                     │
│       ▼                                                     │
│  2. 定位瓶颈    →  剖析分析 / 指标度量 / 对比定位            │
│       │                                                     │
│       ▼                                                     │
│  3. 分析原因    →  深入代码 / 理解原理 / 找到根因            │
│       │                                                     │
│       ▼                                                     │
│  4. 实施优化    →  制定方案 / 实施变更 / 控制风险            │
│       │                                                     │
│       ▼                                                     │
│  5. 验证效果    →  对比数据 / 功能验证 / 回归测试            │
│       │                                                     │
│       ▼                                                     │
│  6. 持续监控    →  建立监控 / 长期跟踪 / 持续改进            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 性能调优的层次

```
┌─────────────────────────────────────────────────────────┐
│  应用层优化（SQL 优化、缓存、连接池）                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │  数据库层优化（索引、表结构、参数调优）          │   │
│  │  ┌─────────────────────────────────────────┐   │   │
│  │  │  存储引擎优化（InnoDB 参数、日志配置）   │   │   │
│  │  │  ┌─────────────────────────────────┐   │   │   │
│  │  │  │  操作系统优化（内存、I/O、网络） │   │   │   │
│  │  │  │  ┌─────────────────────────┐   │   │   │   │
│  │  │  │  │  硬件优化（CPU、内存、磁盘）│   │   │   │   │
│  │  │  │  └─────────────────────────┘   │   │   │   │
│  │  │  └─────────────────────────────────┘   │   │   │
│  │  └─────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**原则：从上层到下层逐层优化，优先解决最容易见效的问题。**

### 1.3 性能调优的常见误区

| 误区 | 说明 | 正确做法 |
|------|------|----------|
| 过度调优 | 过早进行底层调优 | 先优化 SQL 和索引 |
| 盲目调优 | 不测量就调整参数 | 先测量，再优化 |
| 忽略整体 | 只优化某个指标 | 关注整体性能 |
| 过度缓存 | 缓存所有数据 | 缓存热点数据 |
| 忽视监控 | 调优后不跟踪 | 建立长期监控 |

## 二、性能指标与监控

### 2.1 核心性能指标

#### QPS（Queries Per Second）

每秒查询数，衡量数据库的查询处理能力。

```sql
-- 查看当前 QPS
SHOW GLOBAL STATUS LIKE 'Queries';
-- 每秒查询数 = (当前 Queries - 上次 Queries) / 时间间隔
```

#### TPS（Transactions Per Second）

每秒事务数，衡量数据库的事务处理能力。

```sql
-- 查看 TPS
SHOW GLOBAL STATUS LIKE 'Com_commit';
SHOW GLOBAL STATUS LIKE 'Com_rollback';
-- TPS = (Com_commit + Com_rollback) / 时间间隔
```

#### 响应时间

响应时间是用户最直接感知的性能指标：

| 指标 | 说明 |
|------|------|
| **平均响应时间** | 所有请求的平均耗时 |
| **P95 响应时间** | 95% 的请求耗时 |
| **P99 响应时间** | 99% 的请求耗时 |
| **最大响应时间** | 最慢的请求耗时 |

**P99 响应时间比平均响应时间更重要**，因为少量慢请求会严重影响用户体验。

#### 并发数

同时处理的请求数量，反映系统的并发处理能力。

```sql
-- 查看当前连接数
SHOW GLOBAL STATUS LIKE 'Threads_connected';
SHOW GLOBAL STATUS LIKE 'Threads_running';

-- 查看最大连接数
SHOW GLOBAL VARIABLES LIKE 'max_connections';
```

#### 缓存命中率

缓存命中率反映了 Buffer Pool 的使用效率。

```sql
-- 查看 InnoDB Buffer Pool 命中率
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_read%';

-- 计算命中率
-- Buffer Pool 命中率 = 
--   Innodb_buffer_pool_read_requests / 
--   (Innodb_buffer_pool_read_requests + Innodb_buffer_pool_reads) * 100%
```

**理想值：> 99%**，低于 99% 说明 Buffer Pool 大小不足或工作集设置不合理。

### 2.2 MySQL 监控体系

#### 基础监控指标

```sql
-- 连接数监控
SHOW GLOBAL STATUS LIKE 'Threads%';
-- Threads_connected: 当前连接数
-- Threads_running: 正在执行查询的连接数
-- Threads_created: 累计创建的线程数
-- Threads_cached: 缓存的线程数

-- 慢查询监控
SHOW GLOBAL STATUS LIKE 'Slow_queries';
SHOW VARIABLES LIKE 'long_query_time';
SHOW VARIABLES LIKE 'slow_query_log';

-- 锁等待监控
SHOW GLOBAL STATUS LIKE 'Innodb_row_lock%';
-- Innodb_row_lock_time: 行锁等待总时间
-- Innodb_row_lock_waits: 行锁等待次数

-- 临时表监控
SHOW GLOBAL STATUS LIKE 'Created_tmp%';
-- Created_tmp_tables: 内存临时表数
-- Created_tmp_disk_tables: 磁盘临时表数（应尽量为0）
```

#### 性能视图

```sql
-- 查看正在运行的线程
SELECT * FROM information_schema.processlist;

-- 查看 InnoDB 引擎状态
SHOW ENGINE INNODB STATUS;

-- 查看表的使用统计
SELECT * FROM information_schema.table_statistics;

-- MySQL 5.7+ 性能模式
SELECT * FROM performance_schema.events_statements_summary_by_digest;
```

#### 监控工具

| 工具 | 说明 |
|------|------|
| **MySQL Enterprise Monitor** | MySQL 官方监控方案 |
| **Percona Monitoring and Management (PMM)** | 开源的 MySQL 监控方案 |
| **Prometheus + Grafana** | 通用监控方案，配合 mysqld_exporter |
| **Zabbix** | 企业级监控方案 |
| **SkyWalking** | 分布式链路追踪 |

### 2.3 监控指标体系

```
┌─────────────────────────────────────────────────────────────┐
│                    MySQL 监控指标体系                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 连接层指标                                              │
│     ├── 活跃连接数 / 总连接数                                │
│     ├── 连接等待数                                          │
│     └── 连接缓存命中率                                      │
│                                                             │
│  2. 查询层指标                                              │
│     ├── QPS / TPS                                           │
│     ├── 慢查询数                                            │
│     ├── 全表扫描数                                          │
│     └── 临时表创建数                                        │
│                                                             │
│  3. 存储引擎指标                                            │
│     ├── Buffer Pool 命中率                                  │
│     ├── 行锁等待                                            │
│     ├── Redo Log 等待                                       │
│     └── 脏页比例                                            │
│                                                             │
│  4. 复制层指标                                              │
│     ├── 主从延迟                                            │
│     ├── IO 线程状态                                         │
│     └── SQL 线程状态                                        │
│                                                             │
│  5. 资源层指标                                              │
│     ├── CPU 使用率                                          │
│     ├── 内存使用率                                          │
│     ├── 磁盘 I/O                                            │
│     └── 网络流量                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 三、CPU 调优

### 3.1 CPU 与 MySQL 的关系

CPU 是 MySQL 性能的核心瓶颈之一。CPU 使用率过高通常意味着：

- 查询需要大量计算（如复杂 JOIN、聚合）
- 临时表操作过多
- 锁竞争严重
- 连接数过多，线程切换频繁

#### 监控 CPU 使用

```bash
# 查看 CPU 使用率
top -bn1 | grep "Cpu(s)"

# 查看 MySQL 进程的 CPU 使用
top -b -n 1 -p $(pgrep -f mysql)

# 使用 vmstat 监控
vmstat 1 5
```

### 3.2 线程池配置

#### 线程池 vs 每连接一线程

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| 每连接一线程 | 每个连接创建一个线程 | 连接数少，简单场景 |
| 线程池 | 多个连接共享线程池 | 高并发场景 |

MySQL 默认使用"每连接一线程"模式。当连接数过多时，会导致：
- 线程切换开销大
- 上下文切换浪费 CPU
- 内存占用过高

#### 线程池配置

```sql
-- MySQL 8.0+ 线程池
INSTALL PLUGIN thread_soname = 'thread_pool.so';

-- 配置线程池大小
SET GLOBAL thread_pool_size = 16;  -- 一般设置为 CPU 核数

-- 配置其他参数
SET GLOBAL thread_pool_oversubscribe = 3;
SET GLOBAL thread_pool_high_priority_connection = 20;
```

#### 连接数配置

```sql
-- 最大连接数
SET GLOBAL max_connections = 500;

-- 等待队列大小
SET GLOBAL back_log = 256;

-- 连接超时
SET GLOBAL wait_timeout = 28800;
SET GLOBAL interactive_timeout = 28800;
SET GLOBAL connect_timeout = 10;

-- 跳过 DNS 解析
SET GLOBAL skip-name-resolve = ON;
```

#### 线程缓存

```sql
-- 线程缓存大小
SET GLOBAL thread_cache_size = 100;

-- 监控线程缓存
SHOW GLOBAL STATUS LIKE 'Threads_created';
SHOW GLOBAL STATUS LIKE 'Threads_cached';
```

如果 `Threads_created` 持续增长，说明 `thread_cache_size` 设置过小，应该调大。

### 3.3 慢查询分析

慢查询是 CPU 高使用率的常见原因。

```sql
-- 开启慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 0.1;
SET GLOBAL slow_query_log_file = '/var/log/mysql/slow.log';

-- 查看慢查询统计
SELECT * FROM mysql.slow_log 
WHERE start_time > DATE_SUB(NOW(), INTERVAL 1 DAY)
ORDER BY query_time DESC
LIMIT 10;

-- 使用 performance_schema 分析
SELECT 
    DIGEST_TEXT,
    COUNT_STAR AS exec_count,
    AVG_TIMER_WAIT / 1000000000 AS avg_time_sec,
    SUM_TIMER_WAIT / 1000000000 AS total_time_sec
FROM performance_schema.events_statements_summary_by_digest
ORDER BY SUM_TIMER_WAIT DESC
LIMIT 10;
```

## 四、内存调优

### 4.1 内存分配总览

```
┌─────────────────────────────────────────────────────────────┐
│                    MySQL 内存分配                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  全局共享内存：                                             │
│  ├── InnoDB Buffer Pool（最大，占物理内存的 50-70%）        │
│  ├── InnoDB Log Buffer（通常 8-16MB）                      │
│  ├── InnoDB Dictionary Memory                              │
│  ├── Key Buffer（MyISAM 表使用）                           │
│  └── Query Cache（8.0 已移除）                             │
│                                                             │
│  会话级内存：                                               │
│  ├── Sort Buffer（ORDER BY / GROUP BY）                    │
│  ├── Join Buffer（关联查询）                               │
│  ├── Read Buffer（顺序扫描）                               │
│  ├── Random Read Buffer（随机扫描）                        │
│  └── Thread Buffer（线程相关）                              │
│                                                             │
│  total_memory = buffer_pool + log_buffer +                │
│                 (connections * (sort_buffer +             │
│                 join_buffer + read_buffer + ...))         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 Buffer Pool 大小设置

#### Buffer Pool 的作用

InnoDB Buffer Pool 是缓存数据和索引的核心内存区域：

- 缓存数据页和索引页
- 减少磁盘 I/O
- 提高查询性能

```
┌─────────────────────────────────────────────────────────────┐
│                    InnoDB Buffer Pool                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   Free List                         │   │
│  │  空闲页链表                                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   LRU List                          │   │
│  │  使用中的页链表（最近使用的在前端）                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   Flush List                        │   │
│  │  脏页链表（需要刷盘的页）                            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Buffer Pool 命中率 = 命中次数 / 总访问次数               │
│  理想值 > 99%                                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 推荐设置

| 系统角色 | 推荐设置 |
|----------|---------|
| 专用数据库服务器 | 物理内存的 50-70% |
| 混合部署（应用+数据库） | 物理内存的 30-40% |
| 从库（只读） | 物理内存的 50-60% |

```sql
-- 设置 Buffer Pool 大小
SET GLOBAL innodb_buffer_pool_size = 8G;

-- 设置实例数（减少锁竞争）
SET GLOBAL innodb_buffer_pool_instances = 8;

-- 查看 Buffer Pool 使用情况
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool%';
```

#### Buffer Pool 命中率计算

```sql
-- 查看读写请求
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_read_requests';
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_reads';

-- read_requests: 从 Buffer Pool 读取的次数（命中）
-- reads: 从磁盘读取的次数（未命中）
-- 命中率 = read_requests / (read_requests + reads) * 100%
```

#### Buffer Pool 脏页比例

```sql
-- 查看脏页比例
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_pages%';

-- Innodb_buffer_pool_pages_data: 已使用页数
-- Innodb_buffer_pool_pages_dirty: 脏页数
-- 脏页比例 = pages_dirty / pages_data * 100%

-- 脏页比例过高调整
SET GLOBAL innodb_max_dirty_pages_pct = 75;
SET GLOBAL innodb_max_dirty_pages_pct_lwm = 0;
```

### 4.3 连接内存配置

每个数据库连接都会分配独立的内存，需要合理配置会话级内存参数。

#### 主要会话级内存参数

| 参数 | 说明 | 默认值 | 建议值 |
|------|------|--------|--------|
| sort_buffer_size | 排序缓冲区 | 256KB | 256KB - 4MB |
| join_buffer_size | 关联缓冲区 | 256KB | 256KB - 4MB |
| read_buffer_size | 读缓冲区 | 128KB | 128KB - 4MB |
| read_rnd_buffer_size | 随机读缓冲区 | 256KB | 256KB - 4MB |
| thread_stack | 线程栈大小 | 256KB | 256KB |
| net_buffer_length | 连接缓冲区 | 16KB | 16KB |
| max_allowed_packet | 最大包大小 | 4MB | 64MB |

```sql
SET GLOBAL sort_buffer_size = 4 * 1024 * 1024;
SET GLOBAL join_buffer_size = 4 * 1024 * 1024;
SET GLOBAL read_buffer_size = 1 * 1024 * 1024;
SET GLOBAL read_rnd_buffer_size = 1 * 1024 * 1024;
```

#### 内存估算公式

```
总内存 = 
    innodb_buffer_pool_size
    + innodb_log_buffer_size
    + max_connections * (
        sort_buffer_size +
        join_buffer_size +
        read_buffer_size +
        read_rnd_buffer_size +
        thread_stack +
        net_buffer_length
    )
    + 其他内存（OS、其他进程）
```

**注意**：会话级内存参数是每连接独立分配的，`max_connections` 较大时需要调低单个会话的内存。

### 4.4 tmp_table_size 配置

`tmp_table_size` 和 `max_heap_table_size` 共同决定了内存临时表的大小限制。

```sql
-- 临时表超过 tmp_table_size 时会写入磁盘
SET GLOBAL tmp_table_size = 64M;
SET GLOBAL max_heap_table_size = 64M;

-- 监控临时表使用
SHOW GLOBAL STATUS LIKE 'Created_tmp%';
-- Created_tmp_disk_tables: 磁盘临时表数（应尽量为0）
```

**建议**：如果 `Created_tmp_disk_tables` 持续增长，需要增大 `tmp_table_size` 或优化 SQL。

## 五、I/O 调优

### 5.1 磁盘选择（SSD vs HDD）

| 磁盘类型 | 顺序读 | 随机读 | IOPS | 价格 |
|----------|--------|--------|------|------|
| HDD | 200MB/s | 1MB/s | ~100 | 低 |
| SATA SSD | 550MB/s | 350MB/s | ~100K | 中 |
| NVMe SSD | 3500MB/s | 2000MB/s | ~500K | 高 |

**MySQL 对随机读写敏感，强烈推荐使用 SSD。**

### 5.2 日志文件大小

#### InnoDB Redo Log 大小

```sql
-- 查看当前设置
SHOW GLOBAL VARIABLES LIKE 'innodb_log_file_size';
SHOW GLOBAL VARIABLES LIKE 'innodb_log_files_in_group';

-- 设置 Redo Log 大小
SET GLOBAL innodb_log_file_size = 512M;
SET GLOBAL innodb_log_files_in_group = 4;

-- 查看 Redo Log 等待
SHOW GLOBAL STATUS LIKE 'Innodb_log_waits';
```

**建议**：
- OLTP 系统：256MB - 1GB
- 写入密集型：512MB - 2GB
- 如果 `Innodb_log_waits` 持续增长，增大 Redo Log

#### Binlog 文件大小

```sql
SET GLOBAL max_binlog_size = 512M;
SET GLOBAL expire_logs_days = 7;
SHOW BINARY LOGS;
```

### 5.3 双清缓冲设置

#### 什么是双清（Doublewrite）

InnoDB 的双清机制是为了防止部分写（Partial Write）导致的数据损坏。

```
┌─────────────────────────────────────────────────────────────┐
│                    双清（Doublewrite）机制                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  写入路径一：Doublewrite Buffer（共享表空间）                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  1. 先写入 Doublewrite Buffer（内存）               │   │
│  │  2. 从 Doublewrite Buffer 写入共享表空间             │   │
│  │  3. 再从 Doublewrite Buffer 写入表空间文件          │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  写入路径二：直接写入（独立表空间）                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  1. 直接从 Buffer Pool 写入表空间文件               │   │
│  │  2. 不经过 Doublewrite Buffer                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 关闭双清

```sql
-- 查看双清状态
SHOW GLOBAL VARIABLES LIKE 'innodb_doublewrite';

-- 关闭双清（SSD 环境下可考虑）
SET GLOBAL innodb_doublewrite = OFF;
```

**风险**：关闭双清后，如果发生部分写，数据将损坏。只有在以下条件下才建议关闭：
- 使用 SSD 存储
- 有可靠的备份方案
- 能接受极端情况下的数据丢失

#### InnoDB Page Cleaner

```sql
-- Page Cleaner 线程数
SET GLOBAL innodb_page_cleaners = 4;

-- 脏页刷盘阈值
SET GLOBAL innodb_max_dirty_pages_pct = 75;
SET GLOBAL innodb_max_dirty_pages_pct_lwm = 0;
```

### 5.4 RAID 配置

#### RAID 级别选择

| RAID 级别 | 数据保护 | 读写性能 | 空间利用率 | 适用场景 |
|-----------|---------|---------|-----------|----------|
| RAID 0 | 无 | 极高 | 100% | 临时数据、缓存 |
| RAID 1 | 高 | 高 | 50% | 系统盘、关键数据 |
| RAID 5 | 中 | 中 | 67-94% | 通用场景 |
| RAID 10 | 高 | 极高 | 50% | OLTP 数据库 |

#### MySQL 推荐 RAID 配置

- **系统盘**：RAID 1
- **数据盘**：RAID 10
- **日志盘**：RAID 1
- **备份盘**：RAID 5 或更低

#### 文件系统选择

| 文件系统 | 说明 | 推荐场景 |
|----------|------|----------|
| **XFS** | 高性能，支持大文件 | Linux 首选 |
| **ext4** | 兼容性好，成熟稳定 | 通用场景 |
| **ZFS** | 支持快照和压缩 | 备份存储 |

```bash
# 挂载 XFS 文件系统
mkfs.xfs -f /dev/sdb
mount -o noatime,nodiratime /dev/sdb /data
```

**关键挂载选项**：
- `noatime`：不更新文件访问时间（减少 I/O）
- `nodiratime`：不更新目录访问时间

## 六、连接池配置

### 6.1 max_connections

```sql
-- 查看历史最大使用连接数
SHOW GLOBAL STATUS LIKE 'Max_used_connections';

-- OLTP 系统：500 - 2000
SET GLOBAL max_connections = 1000;

-- 配套参数
SET GLOBAL back_log = 128;
SET GLOBAL thread_cache_size = 200;
```

**如果 `Max_used_connections` 接近 `max_connections`，需要增大或优化应用减少连接数。**

### 6.2 超时设置

```sql
-- 连接建立超时
SET GLOBAL connect_timeout = 10;

-- 非交互式连接空闲超时
SET GLOBAL wait_timeout = 28800;

-- 读写操作超时
SET GLOBAL net_read_timeout = 30;
SET GLOBAL net_write_timeout = 60;

-- 锁等待超时
SET GLOBAL innodb_lock_wait_timeout = 50;
```

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| connect_timeout | 10s | 连接建立超时 |
| wait_timeout | 8h | 非交互连接空闲超时 |
| innodb_lock_wait_timeout | 50s | 锁等待超时 |

### 6.3 持久连接

#### 应用层连接池配置（HikariCP）

```java
HikariConfig config = new HikariConfig();
config.setMaximumPoolSize(50);
config.setMinimumIdle(10);
config.setIdleTimeout(600000);
config.setMaxLifetime(1800000);
config.setConnectionTimeout(30000);
config.setPoolName("MySQLPool");

// 预编译语句缓存
config.addDataSourceProperty("cachePrepStmts", "true");
config.addDataSourceProperty("prepStmtCacheSize", "250");
```

#### 连接池最佳实践

1. 连接池大小不要超过 `max_connections`
2. 合理设置超时，避免长时间占用连接
3. 使用连接复用，避免频繁创建和销毁
4. 监控连接池活跃连接数
5. 确保连接使用后正确关闭

## 七、参数调优

### 7.1 innodb_buffer_pool_size

InnoDB Buffer Pool 的大小，用于缓存数据和索引。

| 场景 | 推荐值 |
|------|--------|
| 专用数据库（独占内存） | 物理内存的 50-70% |
| 混合部署 | 物理内存的 30-40% |
| 从库 | 物理内存的 50-60% |

```sql
-- MySQL 5.7+ 支持动态调整
SET GLOBAL innodb_buffer_pool_size = 8G;
```

### 7.2 innodb_log_file_size

Redo Log 文件大小，影响写入性能和恢复时间。

| 场景 | 推荐值 |
|------|--------|
| OLTP（读多写少） | 256MB - 512MB |
| OLTP（写密集） | 512MB - 2GB |

```sql
SET GLOBAL innodb_log_file_size = 512M;
```

### 7.3 innodb_flush_log_at_trx_commit

控制 Redo Log 的刷盘策略。

| 值 | 行为 | 性能 | 数据安全 |
|----|------|------|----------|
| **0** | 每秒刷一次盘 | 最快 | 最差（丢失1秒数据） |
| **1** | 每次事务提交刷盘 | 最慢 | 最好（不丢数据） |
| **2** | 提交写缓存，每秒刷盘 | 中等 | 较好 |

```
innodb_flush_log_at_trx_commit = 1（最安全）
事务提交 → 写入 Redo Log 文件 → 调用 fsync 刷盘 → 返回成功

innodb_flush_log_at_trx_commit = 2（性能与安全平衡）
事务提交 → 写入 Redo Log 文件 → 返回成功
（刷盘由后台线程每秒执行一次）
```

**推荐值**：
- 金融、支付类：1
- 一般业务：2
- 日志、分析类：0

### 7.4 sync_binlog

控制 Binlog 的刷盘策略。

| 值 | 行为 | 性能 | 数据安全 |
|----|------|------|----------|
| **0** | OS 决定刷盘 | 最快 | 最差 |
| **1** | 每次事务提交刷盘 | 最慢 | 最好 |
| **N** | 每 N 个事务刷盘 | 中等 | 较好 |

#### 与 innodb_flush_log_at_trx_commit 的组合

| 组合 | 安全性 | 性能 | 推荐场景 |
|------|--------|------|----------|
| (1, 1) | 最高 | 最低 | 金融系统 |
| (1, 2) | 高 | 中 | 一般业务 |
| (2, 1) | 高 | 中 | 一般业务 |
| (2, 2) | 较高 | 较高 | 非关键业务 |
| (0, 0) | 最低 | 最高 | 日志系统 |

```sql
-- 推荐配置
SET GLOBAL innodb_flush_log_at_trx_commit = 1;
SET GLOBAL sync_binlog = 1;
```

### 7.5 query_cache_size（8.0 已移除）

MySQL 8.0 已移除查询缓存。在 MySQL 5.7 中：

```sql
-- 查看查询缓存状态
SHOW GLOBAL STATUS LIKE 'Qcache%';

-- 如果命中率低于 50%，建议关闭
SET GLOBAL query_cache_size = 0;
SET GLOBAL query_cache_type = 0;
```

**替代方案**：使用应用层缓存（Redis、Memcached）。

### 7.6 其他重要参数

#### innodb_file_per_table

```sql
SET GLOBAL innodb_file_per_table = ON;
```

每个表使用独立的表空间文件，好处：
- 表空间可以独立管理
- 删除大表后空间可以回收
- 便于数据导入导出

#### innodb_flush_method

```sql
-- Linux 推荐 O_DIRECT
SET GLOBAL innodb_flush_method = O_DIRECT;
```

#### table_open_cache

```sql
SET GLOBAL table_open_cache = 4000;

SHOW GLOBAL STATUS LIKE 'Opened_tables';
```

如果 `Opened_tables` 持续增长，说明 `table_open_cache` 设置过小。

#### back_log

```sql
SET GLOBAL back_log = 256;
```

当连接数达到 `max_connections` 时，新的连接请求会进入等待队列。

## 八、高可用架构设计

### 8.1 主从架构

#### 基本主从架构

```
┌──────────────┐                    ┌──────────────┐
│   客户端     │                    │              │
└──────┬───────┘                    │              │
       │                            │              │
       ▼                            │              │
┌──────────────┐   复制(Binlog)    │  从库        │
│   主库       │──────────────────►│              │
│              │                    └──────────────┘
└──────────────┘
```

**优点**：实现简单，提供数据备份和读写分离
**缺点**：主库单点故障，需要手动切换

### 8.2 MHA

MHA（Master High Availability）是最常用的 MySQL 高可用方案之一。

```
┌─────────────────────────────────────────────────────────┐
│                       MHA 架构                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌───────────────────┐                                 │
│  │   MHA Manager     │                                 │
│  │   (管理节点)      │                                 │
│  │   - 监控主库状态  │                                 │
│  │   - 故障转移      │                                 │
│  └────────┬──────────┘                                 │
│           │                                             │
│     监控 & 管理                                         │
│           │                                             │
│  ┌────────┴──────────┐                                 │
│  ▼                    ▼                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   主库       │  │  从库1       │  │  从库2       │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### MHA 工作流程

```
1. MHA Manager 定期检测主库心跳
   ↓
2. 如果主库无响应，判断为故障
   ↓
3. 选择数据最新的从库作为新主库
   ↓
4. 将新主库提升为主库
   ↓
5. 将其他从库指向新主库
   ↓
6. 更新应用配置
   ↓
7. 恢复服务
```

### 8.3 Orchestrator

Orchestrator 是另一个流行的 MySQL 高可用管理工具。

```
┌─────────────────────────────────────────────────────────┐
│                     Orchestrator 架构                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              Orchestrator Server                │   │
│  │  ├── Web UI 管理界面                            │   │
│  │  ├── 自动故障转移                               │   │
│  │  ├── 重建从库                                   │   │
│  │  └── 读写路由                                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              MySQL 集群                         │   │
│  │  ├── 主库 (Primary)                             │   │
│  │  ├── 从库1 (Replica)                            │   │
│  │  └── 从库2 (Replica)                            │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 8.4 三节点集群（Group Replication）

```
┌─────────────────────────────────────────────────────────┐
│              三节点 Group Replication 集群               │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   节点1      │  │   节点2      │  │   节点3      │ │
│  │  (Primary)   │  │  (Secondary) │  │  (Secondary) │ │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘ │
│         └─────────┬───────┴─────────────────┘          │
│                   │                                     │
│            Group Communication                          │
│            (基于 Paxos 协议)                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

#### Group Replication 配置

```ini
# my.cnf
[mysqld]
plugin_load_group="/usr/lib/mysql/plugin/group_replication.so"

group_replication_group_name = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"
group_replication_start_on_boot = OFF
group_replication_group_seeds = "node1:24801,node2:24801,node3:24801"
group_replication_local_address = "node1:24801"
group_replication_single_primary_mode = ON

gtid_mode = ON
enforce_gtid_consistency = ON
server_id = 1
```

### 8.5 高可用方案对比

| 方案 | 复杂度 | 自动故障转移 | RTO | RPO | 适用场景 |
|------|--------|-------------|-----|-----|----------|
| 主从 + VIP | 低 | 手动 | 分钟级 | 秒级 | 小规模 |
| MHA | 中 | 自动 | 30s | 秒级 | 中规模 |
| Orchestrator | 中 | 自动 | 30s | 秒级 | 中大规模 |
| Group Replication | 高 | 自动 | 秒级 | 0 | 大规模 |

## 九、性能测试方法论

### 9.1 性能测试的类型

| 类型 | 说明 | 目的 |
|------|------|------|
| **基准测试** | 测试系统的基本性能 | 建立性能基线 |
| **负载测试** | 模拟正常负载 | 验证系统能否承载 |
| **压力测试** | 逐步加压到极限 | 找到系统极限 |
| **稳定性测试** | 长时间运行 | 发现内存泄漏等问题 |
| **容量规划** | 预测未来容量需求 | 指导硬件采购 |

### 9.2 性能测试流程

```
1. 确定测试目标（QPS、响应时间、并发数）
   ↓
2. 准备测试环境（独立环境、真实数据）
   ↓
3. 编写测试脚本（模拟真实业务）
   ↓
4. 执行测试并收集指标
   ↓
5. 分析测试结果
   ↓
6. 优化并重新测试
   ↓
7. 输出测试报告
```

## 十、压力测试工具

### 10.1 sysbench

sysbench 是 MySQL 官方推荐的基准测试工具。

```bash
# 准备数据
sysbench \
  --db-driver=mysql \
  --mysql-host=127.0.0.1 \
  --mysql-user=root \
  --mysql-password=password \
  --mysql-db=test_db \
  --table-size=1000000 \
  --tables=16 \
  --threads=16 \
  oltp_read_only prepare

# 执行只读测试
sysbench \
  --db-driver=mysql \
  --mysql-host=127.0.0.1 \
  --mysql-user=root \
  --mysql-password=password \
  --mysql-db=test_db \
  --table-size=1000000 \
  --tables=16 \
  --threads=64 \
  --time=60 \
  oltp_read_only run
```

#### sysbench 测试结果解读

```
SQL statistics:
    transactions:                        612258 (10203.63 per sec.)  -- TPS
    queries:                             12245159 (20407.27 per sec.)  -- QPS

Latency (ms):
         min:                                    0.52
         avg:                                    0.98
         max:                                   12.34
         95th percentile:                         1.56
```

### 10.2 JMeter

Apache JMeter 是一个功能强大的性能测试工具。

```bash
# 非 GUI 模式运行测试
jmeter -n -t test_plan.jmx -l result.jtl
```

### 10.3 mysqlslap

MySQL 官方命令行测试工具。

```bash
# 自动生成测试 SQL
mysqlslap -u root -p \
  --auto-generate-sql \
  --concurrency=50 \
  --iterations=100 \
  --number-int-cols=5 \
  --number-char-cols=5
```

## 十一、性能瓶颈定位

### 11.1 瓶颈定位流程图

```
开始
  ↓
是否有监控告警？──否──► 用户反馈性能问题
  │是
  ↓
查看监控指标
  │
  ├── CPU 高？──► 优化 SQL / 减少计算
  ├── 内存高？──► 增大 Buffer Pool / 优化内存配置
  ├── I/O 高？──► 使用 SSD / 调整刷盘参数
  ├── 连接数高？──► 优化连接池 / 增大 max_connections
  └── 锁瓶颈？──► 减小事务范围 / 优化索引
  │
  ↓
定位具体瓶颈 → 针对性优化 → 验证效果
```

### 11.2 常见瓶颈与排查

#### CPU 瓶颈

```sql
-- 查看正在执行的 SQL
SHOW PROCESSLIST;

-- 查看慢查询
SELECT * FROM mysql.slow_log ORDER BY query_time DESC LIMIT 10;

-- 检查临时表
SHOW GLOBAL STATUS LIKE 'Created_tmp%';
```

#### 内存瓶颈

```sql
-- 查看 Buffer Pool 命中率
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_read%';

-- 查看连接数
SHOW GLOBAL STATUS LIKE 'Threads%';
```

#### I/O 瓶颈

```sql
-- 查看 Redo Log 等待
SHOW GLOBAL STATUS LIKE 'Innodb_log_waits';

-- 查看脏页比例
SHOW GLOBAL STATUS LIKE 'Innodb_buffer_pool_pages%';
```

#### 锁瓶颈

```sql
-- 查看锁等待
SHOW GLOBAL STATUS LIKE 'Innodb_row_lock%';

-- 查看当前锁
SELECT * FROM information_schema.innodb_trx;
SELECT * FROM information_schema.innodb_locks;
SELECT * FROM information_schema.innodb_lock_waits;
```

## 十二、实战调优案例

### 案例一：电商系统并发调优

#### 背景

电商系统在大促期间性能瓶颈：QPS 从 3000 降到 500，CPU > 90%。

#### 排查过程

```sql
-- 发现大量全表扫描
EXPLAIN SELECT * FROM orders 
WHERE user_id = 100 AND status = 'paid' 
ORDER BY create_time DESC 
LIMIT 20;
-- type: ALL
```

#### 优化步骤

1. 创建联合索引：`CREATE INDEX idx_user_status_create ON orders(user_id, status, create_time DESC);`
2. 优化 SQL，避免 `SELECT *`
3. 调整 Buffer Pool 大小至 16G
4. 启用读写分离

#### 优化效果

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| QPS | 500 | 8000 | 16x |
| 平均响应时间 | 500ms | 30ms | 16.7x |
| CPU 使用率 | 90% | 40% | - |

### 案例二：日志系统写入优化

#### 优化步骤

1. 调整刷盘策略：`innodb_flush_log_at_trx_commit=0, sync_binlog=0`
2. 增大 Redo Log：`innodb_log_file_size=2G`
3. 使用批量写入
4. 关闭双清（SSD 环境）
5. 文件系统挂载添加 `noatime`

#### 优化效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 写入 QPS | 5000 | 50000 |
| 平均写入延迟 | 200ms | 10ms |

### 案例三：报表查询优化

#### 优化步骤

1. 创建复合索引：`CREATE INDEX idx_time_category ON orders(create_time, category_id, total);`
2. 使用覆盖索引
3. 创建每日汇总表
4. 应用层缓存

#### 优化效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 查询时间 | 30s+ | 200ms |
| CPU 使用率 | 100% | 30% |
| Buffer Pool 命中率 | 90% | 99.9% |

## 十三、总结

MySQL 性能调优是一项系统工程，需要从多个层面入手。核心要点包括：

1. **建立完善的监控体系**：量化的性能指标是调优的基础
2. **遵循科学的调优方法论**：发现 → 定位 → 分析 → 实施 → 验证 → 监控
3. **重视 SQL 和索引优化**：大多数性能问题通过优化 SQL 就能解决
4. **合理配置内存参数**：Buffer Pool 是 InnoDB 性能的核心
5. **优化 I/O 配置**：使用 SSD、调整刷盘策略、RAID 配置
6. **建立高可用架构**：主从复制 + MHA/Orchestrator/Group Replication
7. **定期性能测试**：使用 sysbench、JMeter 等工具进行基准测试
8. **持续监控与改进**：性能调优没有终点，需要持续迭代

通过系统地应用这些调优策略，可以将 MySQL 的性能发挥到极致，支撑从单机到分布式集群的各种业务场景。
