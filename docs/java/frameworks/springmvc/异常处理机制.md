# 异常处理机制

## 一、异常处理概述

### 1.1 为什么需要统一异常处理

在Web应用中，异常处理是保证系统健壮性的关键。如果没有统一的异常处理：

- 代码中充满`try-catch`，冗余且难维护
- 异常响应格式不统一，前端处理困难
- 暴露系统内部信息，存在安全隐患

### 1.2 异常处理方式对比

| 方式 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| 直接try-catch | 每个方法单独处理 | 灵活 | 代码冗余 |
| @ExceptionHandler | 控制器级别 | 粒度细 | 跨控制器重复 |
| @ControllerAdvice | 全局级别 | 统一处理 | 不够灵活 |
| ErrorController | 全局错误页 | 处理404等 | 响应不够精细 |

---

## 二、@ExceptionHandler注解

### 2.1 控制器级别异常处理

```java
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    // 业务方法
    @GetMapping("/{id}")
    public User getUser(@PathVariable Long id) {
        User user = userService.findById(id);
        if (user == null) {
            throw new UserNotFoundException(id);
        }
        return user;
    }
    
    // 异常处理方法
    @ExceptionHandler(UserNotFoundException.class)
    @ResponseStatus(HttpStatus.NOT_FOUND)
    public ErrorResponse handleUserNotFound(
            UserNotFoundException ex) {
        return ErrorResponse.builder()
            .code(404)
            .message("用户不存在")
            .detail(ex.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    @ExceptionHandler(IllegalArgumentException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ErrorResponse handleIllegalArgument(
            IllegalArgumentException ex) {
        return ErrorResponse.builder()
            .code(400)
            .message("参数错误")
            .detail(ex.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
}
```

### 2.2 多异常类型处理

```java
@RestController
public class OrderController {
    
    @PostMapping("/orders")
    public Order createOrder(@RequestBody OrderRequest request) {
        // 可能抛出多种异常
        return orderService.create(request);
    }
    
    // 处理多种异常
    @ExceptionHandler({
        InsufficientStockException.class,
        PriceChangedException.class
    })
    @ResponseStatus(HttpStatus.CONFLICT)
    public ErrorResponse handleOrderConflict(Exception ex) {
        return ErrorResponse.builder()
            .code(409)
            .message("订单冲突")
            .detail(ex.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    // 处理所有未捕获的异常
    @ExceptionHandler(Exception.class)
    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    public ErrorResponse handleGenericException(Exception ex) {
        log.error("系统异常", ex);
        return ErrorResponse.builder()
            .code(500)
            .message("服务器内部错误")
            .detail("系统繁忙，请稍后重试")
            .timestamp(LocalDateTime.now())
            .build();
    }
}
```

### 2.3 返回不同响应类型

```java
@Controller  // 注意：不是@RestController
public class PageController {
    
    // 页面渲染方式
    @GetMapping("/users/{id}")
    public String userDetail(@PathVariable Long id, Model model) {
        User user = userService.findById(id);
        if (user == null) {
            throw new UserNotFoundException(id);
        }
        model.addAttribute("user", user);
        return "user/detail";
    }
    
    // 返回错误页面
    @ExceptionHandler(UserNotFoundException.class)
    public String handleUserNotFound(
            UserNotFoundException ex, Model model) {
        model.addAttribute("error", ex.getMessage());
        return "error/404";  // 返回错误页面视图
    }
    
    // 返回重定向
    @ExceptionHandler(UnauthorizedException.class)
    public String handleUnauthorized() {
        return "redirect:/login";
    }
}
```

---

## 三、@ControllerAdvice全局异常处理

### 3.1 创建全局异常处理器

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    private static final Logger log = 
        LoggerFactory.getLogger(GlobalExceptionHandler.class);
    
    // 处理400 Bad Request
    @ExceptionHandler(MethodArgumentNotValidException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ErrorResponse handleValidationException(
            MethodArgumentNotValidException ex) {
        
        List<FieldError> fieldErrors = ex.getBindingResult()
            .getFieldErrors();
        
        Map<String, String> errors = new HashMap<>();
        for (FieldError error : fieldErrors) {
            errors.put(error.getField(), error.getDefaultMessage());
        }
        
        return ErrorResponse.builder()
            .code(400)
            .message("参数校验失败")
            .errors(errors)
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    // 处理业务异常
    @ExceptionHandler(BusinessException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ErrorResponse handleBusinessException(
            BusinessException ex) {
        
        log.warn("业务异常: {}", ex.getMessage());
        
        return ErrorResponse.builder()
            .code(ex.getCode())
            .message(ex.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    // 处理资源未找到
    @ExceptionHandler(ResourceNotFoundException.class)
    @ResponseStatus(HttpStatus.NOT_FOUND)
    public ErrorResponse handleResourceNotFound(
            ResourceNotFoundException ex) {
        
        return ErrorResponse.builder()
            .code(404)
            .message("资源不存在")
            .detail(ex.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    // 处理无权限
    @ExceptionHandler(AccessDeniedException.class)
    @ResponseStatus(HttpStatus.FORBIDDEN)
    public ErrorResponse handleAccessDenied(
            AccessDeniedException ex) {
        
        return ErrorResponse.builder()
            .code(403)
            .message("无权访问")
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    // 处理未知异常（兜底）
    @ExceptionHandler(Exception.class)
    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
    public ErrorResponse handleException(Exception ex) {
        
        log.error("系统异常", ex);
        
        return ErrorResponse.builder()
            .code(500)
            .message("服务器内部错误")
            .detail("系统繁忙，请稍后重试")
            .timestamp(LocalDateTime.now())
            .build();
    }
}
```

### 3.2 异常处理器精细化控制

```java
@RestControllerAdvice
// 限制只处理特定包下的控制器
// @ControllerAdvice(basePackages = "com.example.controller")
// @ControllerAdvice(annotations = RestController.class)
public class FineGrainedExceptionHandler {
    
    // 方式1：通过包路径限制
    // 只处理com.example.user包下的控制器
    
    // 方式2：通过注解限制
    // 只处理带有@RestController注解的类
    
    // 方式3：组合使用
}
```

### 3.3 多个异常处理器的执行顺序

```java
// 使用@Order注解控制执行顺序
@RestControllerAdvice
@Order(Ordered.HIGHEST_PRECEDENCE)  // 最先执行
public class HighPriorityExceptionHandler {
    // ...
}

@RestControllerAdvice
@Order(Ordered.LOWEST_PRECEDENCE)  // 最后执行
public class LowPriorityExceptionHandler {
    // ...
}
```

---

## 四、自定义异常体系

### 4.1 业务异常基类

```java
// 业务异常基类
public class BusinessException extends RuntimeException {
    
    private final Integer code;
    
    public BusinessException(String message) {
        super(message);
        this.code = 400;
    }
    
    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }
    
    public BusinessException(ErrorCode errorCode) {
        super(errorCode.getMessage());
        this.code = errorCode.getCode();
    }
    
    public Integer getCode() {
        return code;
    }
}

// 错误码枚举
public enum ErrorCode {
    
    // 通用错误
    BAD_REQUEST(400, "请求参数错误"),
    UNAUTHORIZED(401, "未授权"),
    FORBIDDEN(403, "禁止访问"),
    NOT_FOUND(404, "资源不存在"),
    INTERNAL_ERROR(500, "服务器内部错误"),
    
    // 用户相关
    USER_NOT_FOUND(1001, "用户不存在"),
    USER_ALREADY_EXISTS(1002, "用户已存在"),
    PASSWORD_ERROR(1003, "密码错误"),
    
    // 订单相关
    ORDER_NOT_FOUND(2001, "订单不存在"),
    ORDER_CANCELLED(2002, "订单已取消"),
    INSUFFICIENT_STOCK(2003, "库存不足");
    
    private final Integer code;
    private final String message;
    
    ErrorCode(Integer code, String message) {
        this.code = code;
        this.message = message;
    }
    
    public Integer getCode() {
        return code;
    }
    
    public String getMessage() {
        return message;
    }
}
```

### 4.2 具体业务异常

```java
// 用户相关异常
public class UserNotFoundException extends BusinessException {
    
    public UserNotFoundException(Long userId) {
        super(ErrorCode.USER_NOT_FOUND.getCode(), 
            "用户不存在：" + userId);
    }
}

public class UserAlreadyExistsException extends BusinessException {
    
    public UserAlreadyExistsException(String username) {
        super(ErrorCode.USER_ALREADY_EXISTS.getCode(), 
            "用户名已存在：" + username);
    }
}

// 订单相关异常
public class OrderNotFoundException extends BusinessException {
    
    public OrderNotFoundException(Long orderId) {
        super(ErrorCode.ORDER_NOT_FOUND.getCode(), 
            "订单不存在：" + orderId);
    }
}

public class InsufficientStockException extends BusinessException {
    
    public InsufficientStockException(Long productId, int requested, int available) {
        super(ErrorCode.INSUFFICIENT_STOCK.getCode(),
            String.format("商品库存不足，请求：%d，可用：%d", 
                requested, available));
    }
}
```

### 4.3 异常响应统一结构

```java
// 统一错误响应结构
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ErrorResponse {
    
    private Integer code;           // 错误码
    private String message;         // 错误消息
    private String detail;          // 详细信息
    private Map<String, String> errors;  // 字段错误（校验失败时）
    private LocalDateTime timestamp;
    
    // 静态工厂方法
    public static ErrorResponse of(ErrorCode errorCode) {
        return ErrorResponse.builder()
            .code(errorCode.getCode())
            .message(errorCode.getMessage())
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    public static ErrorResponse of(Integer code, String message) {
        return ErrorResponse.builder()
            .code(code)
            .message(message)
            .timestamp(LocalDateTime.now())
            .build();
    }
    
    public static ErrorResponse of(Integer code, String message, 
            Map<String, String> errors) {
        return ErrorResponse.builder()
            .code(code)
            .message(message)
            .errors(errors)
            .timestamp(LocalDateTime.now())
            .build();
    }
}
```

---

## 五、404/500错误页面处理

### 5.1 配置错误页面

#### 方式1：web.xml配置

```xml
<?xml version="1.0" encoding="UTF-8"?>
<web-app>
    
    <!-- 404错误页面 -->
    <error-page>
        <error-code>404</error-code>
        <location>/error/404</location>
    </error-page>
    
    <!-- 500错误页面 -->
    <error-page>
        <error-code>500</error-code>
        <location>/error/500</location>
    </error-page>
    
    <!-- 异常类型错误页面 -->
    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/error/exception</location>
    </error-page>
</web-app>
```

#### 方式2：ErrorController（Spring Boot推荐）

```java
@Controller
public class CustomErrorController implements ErrorController {
    
    private static final String ERROR_PATH = "/error";
    
    // 处理通用错误
    @GetMapping(ERROR_PATH)
    public ResponseEntity<?> handleError(
            HttpServletRequest request) {
        
        Integer status = (Integer) request.getAttribute(
            RequestDispatcher.ERROR_STATUS_CODE);
        String message = (String) request.getAttribute(
            RequestDispatcher.ERROR_MESSAGE);
        String path = (String) request.getAttribute(
            RequestDispatcher.ERROR_REQUEST_URI);
        
        // 根据状态码返回不同响应
        if (status == 404) {
            return ResponseEntity.status(HttpStatus.NOT_FOUND)
                .body(ErrorResponse.of(404, "资源不存在"));
        }
        
        if (status == 405) {
            return ResponseEntity.status(HttpStatus.METHOD_NOT_ALLOWED)
                .body(ErrorResponse.of(405, "请求方法不支持"));
        }
        
        // 默认500错误
        return ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)
            .body(ErrorResponse.of(500, "服务器内部错误"));
    }
    
    // 针对HTML请求返回错误页面
    @GetMapping(value = ERROR_PATH, produces = "text/html")
    public String handleErrorPage(
            HttpServletRequest request, Model model) {
        
        Integer status = (Integer) request.getAttribute(
            RequestDispatcher.ERROR_STATUS_CODE);
        
        model.addAttribute("status", status);
        model.addAttribute("message", 
            request.getAttribute(RequestDispatcher.ERROR_MESSAGE));
        model.addAttribute("path", 
            request.getAttribute(RequestDispatcher.ERROR_REQUEST_URI));
        
        // 根据状态码返回不同视图
        if (status == 404) {
            return "error/404";
        }
        return "error/500";
    }
    
    @Override
    public String getErrorPath() {
        return ERROR_PATH;
    }
}
```

### 5.2 错误页面视图

```html
<!-- error/404.html -->
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>404 - 页面未找到</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: #f5f5f5;
        }
        .error-container {
            text-align: center;
            padding: 40px;
        }
        h1 {
            font-size: 120px;
            color: #e74c3c;
            margin: 0;
        }
        h2 {
            color: #333;
        }
        p {
            color: #666;
        }
        a {
            color: #3498db;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>404</h1>
        <h2>页面未找到</h2>
        <p th:text="'路径：' + ${path}">路径信息</p>
        <a href="/">返回首页</a>
    </div>
</body>
</html>
```

---

## 六、异步与REST异常处理

### 6.1 异步请求异常处理

```java
@RestController
@RequestMapping("/api/async")
public class AsyncController {
    
    @Autowired
    private AsyncService asyncService;
    
    // 基于CompletableFuture的异步
    @GetMapping("/future")
    public CompletableFuture<Result> asyncRequest() {
        return asyncService.processAsync()
            .thenApply(Result::success)
            .exceptionally(ex -> 
                Result.error(500, ex.getMessage()));
    }
    
    // 基于DeferredResult的异步
    @GetMapping("/deferred")
    public DeferredResult<Result> deferredResult() {
        DeferredResult<Result> deferredResult = new DeferredResult<>(5000L);
        
        deferredResult.onTimeout(() -> 
            deferredResult.setResult(Result.error(408, "请求超时")));
        
        deferredResult.onError((Throwable t) -> 
            deferredResult.setResult(Result.error(500, t.getMessage())));
        
        // 异步处理
        asyncService.processAsyncWithCallback(result -> {
            deferredResult.setResult(Result.success(result));
        });
        
        return deferredResult;
    }
}
```

### 6.2 REST API错误规范

```java
// REST API错误响应规范（遵循RFC 7807）
@Data
@Builder
public class ProblemDetail {
    
    private String type;       // 错误类型URL
    private String title;      // 错误标题
    private int status;        // HTTP状态码
    private String detail;     // 详细描述
    private String instance;   // 错误实例URL（可用于追踪）
    private Map<String, Object> extensions;  // 扩展信息
    
    public static ProblemDetail of(
            int status, String title, String detail) {
        return ProblemDetail.builder()
            .type("https://example.com/errors/" + status)
            .title(title)
            .status(status)
            .detail(detail)
            .instance("/api/problems/" + UUID.randomUUID())
            .build();
    }
}

// 使用示例
@RestControllerAdvice
public class RestExceptionHandler {
    
    @ExceptionHandler(ResourceNotFoundException.class)
    @ResponseStatus(HttpStatus.NOT_FOUND)
    public ProblemDetail handleNotFound(
            ResourceNotFoundException ex) {
        
        return ProblemDetail.of(
            404,
            "Resource Not Found",
            ex.getMessage()
        );
    }
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    @ResponseStatus(HttpStatus.BAD_REQUEST)
    public ProblemDetail handleValidation(
            MethodArgumentNotValidException ex) {
        
        Map<String, Object> extensions = new HashMap<>();
        List<Map<String, String>> fieldErrors = new ArrayList<>();
        
        ex.getBindingResult().getFieldErrors().forEach(error -> {
            Map<String, String> fieldError = new HashMap<>();
            fieldError.put("field", error.getField());
            fieldError.put("message", error.getDefaultMessage());
            fieldErrors.add(fieldError);
        });
        
        extensions.put("fieldErrors", fieldErrors);
        
        return ProblemDetail.builder()
            .type("https://example.com/errors/validation")
            .title("Validation Failed")
            .status(400)
            .detail("请求参数校验失败")
            .extensions(extensions)
            .build();
    }
}
```

---

## 七、异常处理最佳实践

### 7.1 设计原则

1. **分层处理**：
   - Controller层：捕获并处理业务异常
   - Service层：抛出明确的业务异常
   - DAO层：抛出数据访问异常

2. **异常信息**：
   - 给用户友好提示（不暴露内部实现）
   - 给开发人员详细日志（便于排查问题）

3. **错误码设计**：
   - 统一的错误码规范
   - 不同业务模块使用不同前缀
   - 便于前端区分处理

### 7.2 完整异常处理链路

```
请求到达Controller
    │
    ├── 1. 参数校验异常 → @ExceptionHandler处理
    │
    ├── 2. 业务异常（Service层抛出）
    │   ├── BusinessException → @ControllerAdvice处理
    │   └── 其他RuntimeException → 日志记录 + 500响应
    │
    ├── 3. 数据异常（DAO层抛出）
    │   └── DataAccessException → 转换为BusinessException
    │
    └── 4. 未捕获异常
        └── 全局ExceptionHandler兜底处理
```

### 7.3 常见问题

#### Q: @ExceptionHandler和@ControllerAdvice的执行顺序？

1. 优先匹配**更具体**的异常类型
2. 同类型异常时，先匹配**Controller本地**的@ExceptionHandler
3. 然后匹配@ControllerAdvice

#### Q: 如何在异常处理中获取请求信息？

```java
@RestControllerAdvice
public class ExceptionHandler {
    
    @ExceptionHandler(Exception.class)
    public ErrorResponse handleException(
            Exception ex,
            HttpServletRequest request,
            @RequestHeader(value = "X-Request-Id", required = false) 
                String requestId) {
        
        // 获取请求信息
        String method = request.getMethod();
        String uri = request.getRequestURI();
        String clientIp = request.getRemoteAddr();
        
        // 记录日志
        log.error("请求异常 - ID: {}, Method: {}, URI: {}, IP: {}", 
            requestId, method, uri, clientIp, ex);
        
        return ErrorResponse.builder()
            .code(500)
            .message("服务器内部错误")
            .detail("请求ID: " + requestId)
            .timestamp(LocalDateTime.now())
            .build();
    }
}
```

#### Q: 如何处理Filter/Interceptor中的异常？

```java
// 方案1：在Filter中try-catch
@Component
public class AuthFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain) throws ServletException, IOException {
        
        try {
            // 执行鉴权逻辑
            String token = request.getHeader("Authorization");
            validateToken(token);
            
            filterChain.doFilter(request, response);
        } catch (UnauthorizedException e) {
            // 直接返回错误响应
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                objectMapper.writeValueAsString(
                    ErrorResponse.of(401, e.getMessage())));
        }
    }
}
```

---

## 八、小结

SpringMVC的异常处理机制为构建健壮的Web应用提供了有力支持：

1. **@ExceptionHandler**：控制器级别，粒度细但需要重复配置
2. **@ControllerAdvice**：全局级别，统一处理，推荐使用
3. **自定义异常体系**：通过错误码枚举+自定义异常类，清晰表达业务异常
4. **ErrorController**：处理404、500等系统错误
5. **REST API规范**：遵循Problem Details RFC标准，提供标准化错误响应

最佳实践：
- 统一使用@ControllerAdvice处理全局异常
- 设计清晰的错误码体系
- 给用户友好提示，给开发人员详细日志
- 避免在异常响应中暴露系统内部信息
