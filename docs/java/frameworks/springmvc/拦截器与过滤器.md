# 拦截器与过滤器

## 一、拦截器（Interceptor）

### 1.1 什么是拦截器

SpringMVC的**拦截器（HandlerInterceptor）** 是在请求处理的**特定阶段**进行拦截的机制，类似于Servlet的Filter，但功能更强大。

#### 拦截器 vs 过滤器

| 特性 | Filter | Interceptor |
|------|--------|-------------|
| 规范 | Servlet规范 | SpringMVC规范 |
| 作用范围 | 所有请求（包括静态资源） | 只拦截Controller请求 |
| 执行时机 | 在DispatcherServlet之前 | 在DispatcherServlet内部 |
| 功能 | 编码、压缩、XSS等 | 鉴权、日志、事务等 |
| 获取Bean | 不支持 | 支持依赖注入 |
| 异常处理 | 需手动处理 | 可由Spring管理 |

### 1.2 HandlerInterceptor接口

```java
public interface HandlerInterceptor {
    
    // 前置处理（Controller执行前）
    // 返回true继续执行，false中断请求
    boolean preHandle(HttpServletRequest request,
                     HttpServletResponse response,
                     Object handler) throws Exception;
    
    // 后置处理（Controller执行后，视图渲染前）
    void postHandle(HttpServletRequest request,
                   HttpServletResponse response,
                   Object handler,
                   ModelAndView modelAndView) throws Exception;
    
    // 完成处理（视图渲染后）
    void afterCompletion(HttpServletRequest request,
                        HttpServletResponse response,
                        Object handler,
                        Exception ex) throws Exception;
}
```

### 1.3 拦截器生命周期

```
请求到达
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│                    Interceptor生命周期                    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. preHandle() 前置处理                                │
│     ├── 返回true → 继续执行                              │
│     └── 返回false → 中断请求                             │
│              │                                          │
│              ▼                                          │
│  2. Controller方法执行                                   │
│              │                                          │
│              ▼                                          │
│  3. postHandle() 后置处理                               │
│     ├── 可修改ModelAndView                               │
│     └── 可添加额外数据                                   │
│              │                                          │
│              ▼                                          │
│  4. 视图渲染                                            │
│              │                                          │
│              ▼                                          │
│  5. afterCompletion() 完成处理                          │
│     ├── 资源清理                                        │
│     └── 异常处理                                        │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 1.4 常用拦截器实现

#### 日志拦截器

```java
@Component
public class LoggingInterceptor implements HandlerInterceptor {
    
    private static final Logger log = 
        LoggerFactory.getLogger(LoggingInterceptor.class);
    
    @Override
    public boolean preHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler) {
        
        // 记录请求开始时间
        request.setAttribute("startTime", System.currentTimeMillis());
        
        log.info("请求开始 - Method: {}, URI: {}, IP: {}",
            request.getMethod(),
            request.getRequestURI(),
            request.getRemoteAddr());
        
        return true;
    }
    
    @Override
    public void postHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler,
            ModelAndView modelAndView) {
        // Controller执行完成，但视图还未渲染
        log.debug("请求处理完成 - URI: {}", request.getRequestURI());
    }
    
    @Override
    public void afterCompletion(HttpServletRequest request,
            HttpServletResponse response, Object handler,
            Exception ex) {
        
        long startTime = (Long) request.getAttribute("startTime");
        long duration = System.currentTimeMillis() - startTime;
        
        // 记录请求完成信息
        if (ex != null) {
            log.error("请求异常 - URI: {}, Duration: {}ms, Error: {}",
                request.getRequestURI(), duration, ex.getMessage());
        } else {
            log.info("请求完成 - URI: {}, Status: {}, Duration: {}ms",
                request.getRequestURI(),
                response.getStatus(),
                duration);
        }
    }
}
```

#### 登录鉴权拦截器

```java
@Component
public class AuthInterceptor implements HandlerInterceptor {
    
    @Autowired
    private UserService userService;
    
    @Autowired
    private JwtTokenUtil jwtTokenUtil;
    
    @Override
    public boolean preHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler) 
            throws Exception {
        
        // 1. 白名单路径放行
        String uri = request.getRequestURI();
        if (isWhitelisted(uri)) {
            return true;
        }
        
        // 2. 获取Token
        String token = request.getHeader("Authorization");
        if (token != null && token.startsWith("Bearer ")) {
            token = token.substring(7);
        }
        
        // 3. 验证Token
        if (token == null || !jwtTokenUtil.validateToken(token)) {
            // Token无效，返回401
            response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                objectMapper.writeValueAsString(
                    ErrorResponse.of(401, "未登录或Token已过期")));
            return false;
        }
        
        // 4. 获取用户信息
        Long userId = jwtTokenUtil.getUserIdFromToken(token);
        User user = userService.findById(userId);
        
        if (user == null || !user.isActive()) {
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                objectMapper.writeValueAsString(
                    ErrorResponse.of(403, "用户已被禁用")));
            return false;
        }
        
        // 5. 将用户信息存入请求属性
        request.setAttribute("currentUser", user);
        SecurityContextHolder.setCurrentUser(user);
        
        return true;
    }
    
    private boolean isWhitelisted(String uri) {
        List<String> whitelist = Arrays.asList(
            "/api/auth/login",
            "/api/auth/register",
            "/api/public/**",
            "/error"
        );
        
        for (String pattern : whitelist) {
            if (match(uri, pattern)) {
                return true;
            }
        }
        return false;
    }
    
    private boolean match(String uri, String pattern) {
        // 简单的通配符匹配
        if (pattern.endsWith("/**")) {
            String prefix = pattern.substring(0, pattern.length() - 3);
            return uri.startsWith(prefix);
        }
        return uri.equals(pattern);
    }
}
```

#### 权限校验拦截器

```java
@Component
public class PermissionInterceptor implements HandlerInterceptor {
    
    @Autowired
    private PermissionService permissionService;
    
    @Override
    public boolean preHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler) 
            throws Exception {
        
        // 1. 获取当前用户
        User user = (User) request.getAttribute("currentUser");
        if (user == null) {
            return true;  // 未登录，由AuthInterceptor处理
        }
        
        // 2. 获取请求的权限标识
        String method = request.getMethod();
        String uri = request.getRequestURI();
        String permission = method.toLowerCase() + ":" + uri;
        
        // 3. 检查权限
        if (!permissionService.hasPermission(user.getId(), permission)) {
            response.setStatus(HttpServletResponse.SC_FORBIDDEN);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                objectMapper.writeValueAsString(
                    ErrorResponse.of(403, "无权访问：" + permission)));
            return false;
        }
        
        return true;
    }
}
```

#### 跨域拦截器

```java
@Component
public class CorsInterceptor implements HandlerInterceptor {
    
    @Override
    public boolean preHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler) {
        
        // 设置CORS响应头
        response.setHeader("Access-Control-Allow-Origin", "*");
        response.setHeader("Access-Control-Allow-Methods",
            "GET, POST, PUT, DELETE, OPTIONS, PATCH");
        response.setHeader("Access-Control-Allow-Headers",
            "Content-Type, Authorization, X-Request-Id");
        response.setHeader("Access-Control-Max-Age", "3600");
        response.setHeader("Access-Control-Allow-Credentials", "true");
        
        // 处理OPTIONS预检请求
        if ("OPTIONS".equalsIgnoreCase(request.getMethod())) {
            response.setStatus(HttpServletResponse.SC_OK);
            return true;
        }
        
        return true;
    }
}
```

### 1.5 注册和配置拦截器

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Autowired
    private LoggingInterceptor loggingInterceptor;
    
    @Autowired
    private AuthInterceptor authInterceptor;
    
    @Autowired
    private PermissionInterceptor permissionInterceptor;
    
    @Autowired
    private CorsInterceptor corsInterceptor;
    
    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        
        // 1. 日志拦截器（所有请求）
        registry.addInterceptor(loggingInterceptor)
            .addPathPatterns("/**")
            .excludePathPatterns("/static/**", "/error");
        
        // 2. 跨域拦截器（所有请求）
        registry.addInterceptor(corsInterceptor)
            .addPathPatterns("/**");
        
        // 3. 鉴权拦截器（排除登录注册等）
        registry.addInterceptor(authInterceptor)
            .addPathPatterns("/api/**")
            .excludePathPatterns(
                "/api/auth/login",
                "/api/auth/register",
                "/api/public/**"
            );
        
        // 4. 权限拦截器（需要登录的接口）
        registry.addInterceptor(permissionInterceptor)
            .addPathPatterns("/api/**")
            .excludePathPatterns(
                "/api/auth/**",
                "/api/public/**"
            )
            .order(2);  // 数字越小优先级越高
    }
}
```

### 1.6 多拦截器执行顺序

```
请求 /api/users
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│                    多拦截器执行顺序                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. CorsInterceptor (order=0)                          │
│     ├── preHandle() ✅                                  │
│     └── afterCompletion()                               │
│                                                         │
│  2. LoggingInterceptor (order=1)                       │
│     ├── preHandle() ✅                                  │
│     └── afterCompletion()                               │
│                                                         │
│  3. AuthInterceptor (order=2)                           │
│     ├── preHandle() ✅                                  │
│     └── afterCompletion()                               │
│                                                         │
│  4. PermissionInterceptor (order=3)                    │
│     ├── preHandle() ✅                                  │
│     └── afterCompletion()                               │
│                                                         │
│  5. Controller执行                                      │
│                                                         │
│  postHandle() 执行顺序与preHandle相反                    │
│  afterCompletion() 执行顺序与preHandle相反               │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 二、过滤器（Filter）

### 2.1 什么是过滤器

Servlet的**过滤器（Filter）** 是在请求到达Servlet之前或响应返回客户端之前进行处理的组件。

```java
public interface Filter {
    
    // 初始化方法（容器创建时调用一次）
    void init(FilterConfig filterConfig) throws ServletException;
    
    // 过滤方法（每次请求都调用）
    void doFilter(ServletRequest request,
                 ServletResponse response,
                 FilterChain chain) throws IOException, ServletException;
    
    // 销毁方法（容器销毁时调用一次）
    void destroy();
}
```

### 2.2 过滤器生命周期

```
容器启动
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│                    Filter生命周期                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. init() 初始化                                       │
│     ├── 容器创建Filter实例                              │
│     └── 读取web.xml配置参数                             │
│              │                                          │
│              ▼                                          │
│  2. doFilter() 过滤（每次请求）                          │
│     ├── 执行过滤逻辑                                    │
│     ├── chain.doFilter() 传递请求                       │
│     └── 执行业务逻辑                                    │
│              │                                          │
│              ▼                                          │
│  3. destroy() 销毁                                      │
│     └── 容器销毁Filter实例                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2.3 常用过滤器实现

#### 编码过滤器

```java
@Component
public class CharacterEncodingFilter implements Filter {
    
    private String encoding = "UTF-8";
    private boolean forceEncoding = true;
    
    @Override
    public void init(FilterConfig filterConfig) 
            throws ServletException {
        // 读取配置参数
        String encodingParam = filterConfig.getInitParameter("encoding");
        if (encodingParam != null) {
            this.encoding = encodingParam;
        }
    }
    
    @Override
    public void doFilter(ServletRequest request,
            ServletResponse response, FilterChain chain) 
            throws IOException, ServletException {
        
        // 设置请求编码
        if (this.encoding != null && 
                (this.forceEncoding || 
                 request.getCharacterEncoding() == null)) {
            request.setCharacterEncoding(this.encoding);
        }
        
        // 设置响应编码
        response.setCharacterEncoding(this.encoding);
        response.setContentType("text/html;charset=" + this.encoding);
        
        chain.doFilter(request, response);
    }
    
    @Override
    public void destroy() {
        // 清理资源
    }
}
```

#### XSS过滤器

```java
@Component
public class XssFilter implements Filter {
    
    @Override
    public void init(FilterConfig filterConfig) {
        // 初始化
    }
    
    @Override
    public void doFilter(ServletRequest request,
            ServletResponse response, FilterChain chain) 
            throws IOException, ServletException {
        
        // 包装请求，过滤XSS字符
        HttpServletRequest httpRequest = (HttpServletRequest) request;
        XssRequestWrapper wrappedRequest = new XssRequestWrapper(httpRequest);
        
        chain.doFilter(wrappedRequest, response);
    }
    
    @Override
    public void destroy() {
        // 清理
    }
}

// XSS请求包装类
public class XssRequestWrapper extends HttpServletRequestWrapper {
    
    private static final Pattern[] XSS_PATTERNS = {
        Pattern.compile("<script[^>]*>.*?</script>", 
            Pattern.CASE_INSENSITIVE),
        Pattern.compile("javascript\\s*:", 
            Pattern.CASE_INSENSITIVE),
        Pattern.compile("on\\w+\\s*=", 
            Pattern.CASE_INSENSITIVE),
        Pattern.compile("<\\s*iframe", 
            Pattern.CASE_INSENSITIVE),
        Pattern.compile("<\\s*img[^>]*on\\w+\\s*=", 
            Pattern.CASE_INSENSITIVE),
    };
    
    public XssRequestWrapper(HttpServletRequest request) {
        super(request);
    }
    
    @Override
    public String getParameter(String name) {
        String value = super.getParameter(name);
        return filterXss(value);
    }
    
    @Override
    public String[] getParameterValues(String name) {
        String[] values = super.getParameterValues(name);
        if (values != null) {
            for (int i = 0; i < values.length; i++) {
                values[i] = filterXss(values[i]);
            }
        }
        return values;
    }
    
    @Override
    public String getHeader(String name) {
        String value = super.getHeader(name);
        return filterXss(value);
    }
    
    private String filterXss(String value) {
        if (value == null || value.isEmpty()) {
            return value;
        }
        
        for (Pattern pattern : XSS_PATTERNS) {
            value = pattern.matcher(value).replaceAll("");
        }
        
        return value;
    }
}
```

#### 请求限流过滤器

```java
@Component
public class RateLimitFilter implements Filter {
    
    private final LoadingCache<String, AtomicInteger> requestCounts;
    private static final int MAX_REQUESTS_PER_MINUTE = 60;
    
    public RateLimitFilter() {
        this.requestCounts = CacheBuilder.newBuilder()
            .expireAfterWrite(1, TimeUnit.MINUTES)
            .build(key -> new AtomicInteger(0));
    }
    
    @Override
    public void doFilter(ServletRequest request,
            ServletResponse response, FilterChain chain) 
            throws IOException, ServletException {
        
        HttpServletRequest httpRequest = (HttpServletRequest) request;
        String clientIp = getClientIp(httpRequest);
        
        // 获取当前IP的请求计数
        AtomicInteger count = requestCounts.get(clientIp);
        int currentCount = count.incrementAndGet();
        
        // 检查是否超过限流阈值
        if (currentCount > MAX_REQUESTS_PER_MINUTE) {
            HttpServletResponse httpResponse = (HttpServletResponse) response;
            httpResponse.setStatus(HttpServletResponse.SC_TOO_MANY_REQUESTS);
            httpResponse.setContentType("application/json;charset=UTF-8");
            httpResponse.getWriter().write(
                "{\"code\":429,\"message\":\"请求过于频繁，请稍后重试\"}");
            return;
        }
        
        // 设置限流响应头
        HttpServletResponse httpResponse = (HttpServletResponse) response;
        httpResponse.setHeader("X-RateLimit-Limit", 
            String.valueOf(MAX_REQUESTS_PER_MINUTE));
        httpResponse.setHeader("X-RateLimit-Remaining", 
            String.valueOf(MAX_REQUESTS_PER_MINUTE - currentCount));
        
        chain.doFilter(request, response);
    }
    
    private String getClientIp(HttpServletRequest request) {
        String ip = request.getHeader("X-Forwarded-For");
        if (ip == null || ip.isEmpty()) {
            ip = request.getHeader("X-Real-IP");
        }
        if (ip == null || ip.isEmpty()) {
            ip = request.getRemoteAddr();
        }
        return ip.split(",")[0].trim();
    }
}
```

### 2.4 注册过滤器

#### 方式1：@Component自动注册

```java
// 直接使用@Component注解，Spring Boot自动注册
@Component
@Order(1)  // 执行顺序
public class MyFilter implements Filter {
    // ...
}
```

#### 方式2：FilterRegistrationBean（推荐）

```java
@Configuration
public class FilterConfig {
    
    @Bean
    public FilterRegistrationBean<CharacterEncodingFilter> 
            characterEncodingFilter() {
        
        FilterRegistrationBean<CharacterEncodingFilter> registration = 
            new FilterRegistrationBean<>();
        
        registration.setFilter(new CharacterEncodingFilter());
        registration.addUrlPatterns("/*");
        registration.addInitParameter("encoding", "UTF-8");
        registration.setOrder(1);
        registration.setName("characterEncodingFilter");
        
        return registration;
    }
    
    @Bean
    public FilterRegistrationBean<XssFilter> xssFilter() {
        FilterRegistrationBean<XssFilter> registration = 
            new FilterRegistrationBean<>();
        
        registration.setFilter(new XssFilter());
        registration.addUrlPatterns("/*");
        registration.setOrder(2);
        registration.setName("xssFilter");
        
        return registration;
    }
    
    @Bean
    public FilterRegistrationBean<RateLimitFilter> rateLimitFilter() {
        FilterRegistrationBean<RateLimitFilter> registration = 
            new FilterRegistrationBean<>();
        
        registration.setFilter(new RateLimitFilter());
        registration.addUrlPatterns("/api/*");
        registration.setOrder(3);
        registration.setName("rateLimitFilter");
        
        return registration;
    }
}
```

#### 方式3：web.xml配置

```xml
<?xml version="1.0" encoding="UTF-8"?>
<web-app>
    
    <filter>
        <filter-name>characterEncodingFilter</filter-name>
        <filter-class>
            org.springframework.web.filter.CharacterEncodingFilter
        </filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>characterEncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    
</web-app>
```

---

## 三、过滤器 vs 拦截器选择

### 3.1 选择建议

| 场景 | 推荐方案 | 原因 |
|------|---------|------|
| 编码处理 | Filter | Servlet规范，所有请求生效 |
| XSS过滤 | Filter | 在最外层拦截，保护所有请求 |
| 压缩处理 | Filter | 处理响应体，减少传输数据 |
| 限流控制 | Filter | 在请求最前面限制流量 |
| 跨域处理 | Interceptor | 需要Spring管理，方便调试 |
| 日志记录 | Interceptor | 可以获取Controller信息 |
| 鉴权校验 | Interceptor | 支持依赖注入，方便获取用户信息 |
| 权限检查 | Interceptor | 可以获取方法信息进行细粒度控制 |
| 事务管理 | Interceptor | 配合@Transactional使用 |

### 3.2 执行顺序

```
请求到达
    │
    ▼
Filter 1 (编码)
    │
    ▼
Filter 2 (XSS过滤)
    │
    ▼
Filter 3 (压缩)
    │
    ▼
┌─────────────────────────────────────────┐
│  DispatcherServlet                       │
│    │                                     │
│    ▼                                     │
│  Interceptor 1 (跨域)                    │
│    │                                     │
│    ▼                                     │
│  Interceptor 2 (日志)                    │
│    │                                     │
│    ▼                                     │
│  Interceptor 3 (鉴权)                    │
│    │                                     │
│    ▼                                     │
│  Controller执行                          │
│    │                                     │
│    ▼                                     │
│  Interceptor.afterCompletion()           │
│                                         │
└─────────────────────────────────────────┘
    │
    ▼
Filter 1.dofilter() ← 返回时执行（逆序）
    │
    ▼
Filter 2.dofilter()
    │
    ▼
Filter 3.dofilter()
    │
    ▼
响应返回客户端
```

### 3.3 对比总结

| 特性 | Filter | Interceptor |
|------|--------|-------------|
| 规范 | Servlet规范 | SpringMVC规范 |
| 作用层次 | Servlet容器 | SpringMVC框架 |
| 适用范围 | 所有请求（包括静态资源） | 只Controller请求 |
| 依赖注入 | ❌ 不支持 | ✅ 支持@Autowired |
| 获取Controller | ❌ 不支持 | ✅ 支持 |
| 获取参数 | Servlet API | Spring参数绑定 |
| 异常处理 | try-catch | Spring统一处理 |
| 配置复杂度 | 较简单 | 稍复杂 |
| 执行时机 | 更早（DispatcherServlet前） | 较晚（DispatcherServlet内） |
| 典型应用 | 编码、压缩、XSS、限流 | 鉴权、日志、权限、事务 |

---

## 四、Spring内置过滤器与拦截器

### 4.1 常用内置过滤器

| 过滤器 | 说明 |
|--------|------|
| `CharacterEncodingFilter` | 字符编码设置 |
| `ShallowEtagHeaderFilter` | ETag缓存支持 |
| `ContentCachingRequestWrapper` | 请求体缓存 |
| `GzipCompressionFilter` | 响应压缩 |
| `OncePerRequestFilter` | 保证只执行一次的过滤器基类 |

### 4.2 常用内置拦截器

| 拦截器 | 说明 |
|--------|------|
| `HandlerMappingInterceptor` | 处理器映射 |
| `ConversionServiceInterceptor` | 类型转换服务 |
| `LocaleChangeInterceptor` | 国际化切换 |
| `ThemeChangeInterceptor` | 主题切换 |
| `MappedInterceptor` | 路径映射拦截器 |

### 4.3 扩展OncePerRequestFilter

```java
// OncePerRequestFilter保证每个请求只执行一次
public abstract class OncePerRequestFilter implements Filter {
    
    public static final String ALREADY_FILTERED_SUFFIX = 
        ".FILTERED";
    
    @Override
    public final void doFilter(ServletRequest request,
            ServletResponse response, FilterChain filterChain) 
            throws ServletException, IOException {
        
        // 检查是否已经执行过
        if (request.getAttribute(getAlreadyFilteredAttributeName()) 
                != null) {
            // 已经执行过，直接放行
            filterChain.doFilter(request, response);
            return;
        }
        
        // 执行过滤逻辑
        doFilterInternal(request, response, filterChain);
    }
    
    // 子类实现
    protected abstract void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain) 
            throws ServletException, IOException;
}

// 自定义过滤器继承OncePerRequestFilter
@Component
public class MyCustomFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain) 
            throws ServletException, IOException {
        
        // 只执行一次的逻辑
        String requestId = UUID.randomUUID().toString();
        request.setAttribute("requestId", requestId);
        
        // 继续执行
        filterChain.doFilter(request, response);
    }
}
```

---

## 五、实战案例

### 5.1 API版本控制拦截器

```java
@Component
public class ApiVersionInterceptor implements HandlerInterceptor {
    
    private static final Logger log = 
        LoggerFactory.getLogger(ApiVersionInterceptor.class);
    
    @Override
    public boolean preHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler) {
        
        String uri = request.getRequestURI();
        String version = request.getHeader("X-API-Version");
        
        // 如果没有指定版本，使用最新版本
        if (version == null || version.isEmpty()) {
            version = getLatestVersion(uri);
        }
        
        // 验证版本是否支持
        if (!isVersionSupported(uri, version)) {
            response.setStatus(HttpServletResponse.SC_NOT_FOUND);
            response.setContentType("application/json;charset=UTF-8");
            response.getWriter().write(
                "{\"code\":404,\"message\":\"API版本不存在：" + 
                version + "\"}");
            return false;
        }
        
        // 设置版本信息到请求属性
        request.setAttribute("apiVersion", version);
        
        // 修改URI（重写请求路径）
        String newUri = uri.replaceFirst(
            "/api/", "/api/" + version + "/");
        request.getRequestDispatcher(newUri)
            .forward(request, response);
        
        return false;  // 已处理，不需要继续
    }
    
    private String getLatestVersion(String uri) {
        // 从配置中获取最新版本
        return "v2";
    }
    
    private boolean isVersionSupported(String uri, String version) {
        // 检查版本是否在支持列表中
        List<String> supportedVersions = Arrays.asList("v1", "v2");
        return supportedVersions.contains(version);
    }
}
```

### 5.2 数据脱敏拦截器

```java
@Component
public class DataMaskingInterceptor implements HandlerInterceptor {
    
    private static final Map<String, String> MASK_RULES = Map.of(
        "phone", "手机号",
        "idCard", "身份证",
        "email", "邮箱"
    );
    
    @Override
    public void postHandle(HttpServletRequest request,
            HttpServletResponse response, Object handler,
            ModelAndView modelAndView) {
        
        if (modelAndView == null || modelAndView.getModel() == null) {
            return;
        }
        
        // 对Model中的数据进行脱敏处理
        Map<String, Object> model = modelAndView.getModel();
        for (Map.Entry<String, Object> entry : model.entrySet()) {
            Object value = entry.getValue();
            if (value instanceof UserDTO user) {
                // 脱敏处理
                user.setPhone(maskPhone(user.getPhone()));
                user.setIdCard(maskIdCard(user.getIdCard()));
                user.setEmail(maskEmail(user.getEmail()));
            }
        }
    }
    
    private String maskPhone(String phone) {
        if (phone == null || phone.length() < 11) {
            return phone;
        }
        return phone.substring(0, 3) + "****" + 
               phone.substring(7);
    }
    
    private String maskIdCard(String idCard) {
        if (idCard == null || idCard.length() < 18) {
            return idCard;
        }
        return idCard.substring(0, 6) + "********" + 
               idCard.substring(14);
    }
    
    private String maskEmail(String email) {
        if (email == null || !email.contains("@")) {
            return email;
        }
        int atIndex = email.indexOf("@");
        String username = email.substring(0, atIndex);
        String domain = email.substring(atIndex);
        if (username.length() > 2) {
            username = username.charAt(0) + "***" + 
                       username.charAt(username.length() - 1);
        }
        return username + domain;
    }
}
```

### 5.3 请求缓存过滤器

```java
@Component
public class RequestCacheFilter extends OncePerRequestFilter {
    
    private final Cache<String, byte[]> responseCache;
    
    public RequestCacheFilter() {
        this.responseCache = Caffeine.newBuilder()
            .maximumSize(1000)
            .expireAfterWrite(10, TimeUnit.MINUTES)
            .build();
    }
    
    @Override
    protected void doFilterInternal(
            HttpServletRequest request,
            HttpServletResponse response,
            FilterChain filterChain) 
            throws ServletException, IOException {
        
        // 只缓存GET请求
        if (!"GET".equalsIgnoreCase(request.getMethod())) {
            filterChain.doFilter(request, response);
            return;
        }
        
        String cacheKey = getCacheKey(request);
        byte[] cachedResponse = responseCache.getIfPresent(cacheKey);
        
        if (cachedResponse != null) {
            // 返回缓存的响应
            response.setContentType("application/json;charset=UTF-8");
            response.setCharacterEncoding("UTF-8");
            response.getOutputStream().write(cachedResponse);
            response.setHeader("X-Cache", "HIT");
            return;
        }
        
        // 包装响应以捕获输出
        ContentCachingResponseWrapper responseWrapper = 
            new ContentCachingResponseWrapper(response);
        
        filterChain.doFilter(request, responseWrapper);
        
        // 缓存响应
        if (responseWrapper.getStatus() == 200 && 
                isCacheable(request)) {
            responseCache.put(cacheKey, 
                responseWrapper.getContentAsByteArray());
            response.setHeader("X-Cache", "MISS");
        } else {
            response.setHeader("X-Cache", "NO_CACHE");
        }
        
        // 复制响应到原始response
        responseWrapper.copyBodyToResponse();
    }
    
    private String getCacheKey(HttpServletRequest request) {
        String queryString = request.getQueryString();
        String uri = request.getRequestURI();
        if (queryString != null) {
            return uri + "?" + queryString;
        }
        return uri;
    }
    
    private boolean isCacheable(HttpServletRequest request) {
        String uri = request.getRequestURI();
        return uri.startsWith("/api/public/");
    }
}
```

---

## 六、小结

拦截器和过滤器是实现横切关注点的重要机制：

1. **过滤器（Filter）**：
   - Servlet规范，作用于所有请求
   - 适用于编码、压缩、XSS过滤、限流等基础设施
   - 在DispatcherServlet之前执行

2. **拦截器（Interceptor）**：
   - SpringMVC规范，只作用于Controller请求
   - 支持依赖注入，功能更强大
   - 适用于鉴权、日志、权限、事务等业务逻辑
   - 在DispatcherServlet内部执行

3. **选择建议**：
   - 基础设施用Filter（编码、压缩、限流）
   - 业务逻辑用Interceptor（鉴权、日志、权限）
   - 两者配合使用，实现完整的请求处理链路

掌握过滤器和拦截器的使用，能够帮助我们：
- 实现优雅的横切关注点处理
- 构建可扩展的Web应用架构
- 提升系统的安全性和可维护性
