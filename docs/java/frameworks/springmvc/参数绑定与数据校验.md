# 参数绑定与数据校验

## 一、参数绑定机制

### 1.1 参数绑定概述

SpringMVC的**参数绑定（Data Binding）** 是指将HTTP请求参数自动转换为控制器方法参数的过程。

#### 绑定流程

```
HTTP请求参数
    │
    ├── 查询字符串 (query string)
    ├── 表单参数 (form data)
    ├── 路径变量 (path variable)
    ├── 请求体 (request body)
    └── 请求头 (request header)
    │
    ▼
SpringMVC参数绑定
    │
    ├── 1. 参数定位
    │   ├── @RequestParam
    │   ├── @PathVariable
    │   ├── @RequestBody
    │   └── 其他注解
    │
    ├── 2. 类型转换
    │   ├── String → Integer
    │   ├── String → Date
    │   └── String → 自定义类型
    │
    ├── 3. 数据格式化
    │   ├── @DateTimeFormat
    │   ├── @NumberFormat
    │   └── 自定义Formatter
    │
    └── 4. 参数校验
        ├── @NotNull
        ├── @Size
        └── 自定义Validator
```

### 1.2 基本类型绑定

```java
@RestController
@RequestMapping("/api/basic")
public class BasicBindingController {
    
    // 绑定基本类型
    @GetMapping("/types")
    public String basicTypes(
            @RequestParam String name,
            @RequestParam int age,
            @RequestParam double score,
            @RequestParam boolean active,
            @RequestParam long count) {
        
        return String.format("name=%s, age=%d, score=%.1f, active=%b, count=%d",
            name, age, score, active, count);
    }
    
    // 绑定数组
    @GetMapping("/array")
    public String arrayBinding(
            @RequestParam String[] tags,
            @RequestParam List<Integer> ids) {
        
        return String.format("tags=%s, ids=%s", 
            Arrays.toString(tags), ids);
    }
    
    // 绑定枚举
    @GetMapping("/enum")
    public String enumBinding(
            @RequestParam OrderStatus status) {
        
        return "status=" + status.name();
    }
}
```

### 1.3 复杂类型绑定

#### POJO绑定

```java
@RestController
@RequestMapping("/api/pojo")
public class PojoBindingController {
    
    // 绑定POJO对象
    @PostMapping
    public String bindPojo(@RequestParam User user) {
        // 自动将参数绑定到User对象
        return "user=" + user.toString();
    }
    
    // 绑定嵌套对象
    @PostMapping("/nested")
    public String bindNested(@RequestParam Order order) {
        // order.user.name, order.user.email
        return "order=" + order.toString();
    }
    
    // 绑定列表
    @PostMapping("/list")
    public String bindList(
            @RequestBody List<User> users) {
        // JSON数组绑定
        return "users size=" + users.size();
    }
    
    // 绑定Map
    @PostMapping("/map")
    public String bindMap(
            @RequestBody Map<String, Object> data) {
        return "data keys=" + data.keySet();
    }
}

// 示例POJO
public class User {
    private Long id;
    private String name;
    private String email;
    private Integer age;
    private Date birthday;
    private Address address;
    private List<String> hobbies;
    
    // getters and setters
}

public class Address {
    private String province;
    private String city;
    private String street;
    
    // getters and setters
}
```

#### 绑定规则

| 场景 | 参数名 | 绑定目标 | 说明 |
|------|--------|---------|------|
| 基本类型 | `name` | `String name` | 直接绑定 |
| POJO属性 | `user.name` | `User.user.name` | 点号分隔 |
| 嵌套对象 | `order.user.name` | `Order.user.name` | 多层点号 |
| 列表索引 | `users[0].name` | `List<User>[0].name` | 方括号索引 |
| Map键值 | `map[key]` | `Map<String, String>` | 方括号键名 |

---

## 二、类型转换

### 2.1 内置类型转换器

SpringMVC内置了大量类型转换器：

| 源类型 | 目标类型 | 示例 |
|--------|---------|------|
| String | Integer/Long/Double | "123" → 123 |
| String | Boolean | "true" → true |
| String | Date | "2024-01-01" → Date |
| String | BigDecimal | "99.99" → 99.99 |
| String | Enum | "ACTIVE" → OrderStatus.ACTIVE |
| String[] | List/Set | ["1","2"] → [1, 2] |
| MultipartFile | byte[] | 文件上传 → 字节数组 |
| JSON | POJO | `@RequestBody User` |

### 2.2 自定义类型转换器

#### 实现Converter接口

```java
// 1. 创建自定义转换器
public class StringToLocalDateConverter 
        implements Converter<String, LocalDate> {
    
    private final DateTimeFormatter formatter;
    
    public StringToLocalDateConverter(String pattern) {
        this.formatter = DateTimeFormatter.ofPattern(pattern);
    }
    
    @Override
    public LocalDate convert(String source) {
        if (source == null || source.isEmpty()) {
            return null;
        }
        return LocalDate.parse(source, formatter);
    }
}

// 2. 注册转换器
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Override
    public void addFormatters(FormatterRegistry registry) {
        // 注册自定义转换器
        registry.addConverter(
            new StringToLocalDateConverter("yyyy-MM-dd"));
    }
}
```

#### 实现Formatter接口（带Locale）

```java
public class PhoneNumberFormatter 
        implements Formatter<PhoneNumber> {
    
    @Override
    public PhoneNumber parse(String text, Locale locale) {
        // 解析 "138-0000-0000" 或 "13800000000"
        String digits = text.replaceAll("[^\\d]", "");
        if (digits.length() == 11) {
            return new PhoneNumber(
                digits.substring(0, 3),
                digits.substring(3, 7),
                digits.substring(7)
            );
        }
        throw new IllegalArgumentException("Invalid phone number: " + text);
    }
    
    @Override
    public String print(PhoneNumber phone, Locale locale) {
        return String.format("%s-%s-%s", 
            phone.getPrefix(), 
            phone.getMiddle(), 
            phone.getSuffix());
    }
}

// 注册Formatter
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Override
    public void addFormatters(FormatterRegistry registry) {
        registry.addFormatter(new PhoneNumberFormatter());
    }
}
```

#### 使用ConversionService

```java
@Service
public class ConversionServiceExample {
    
    @Autowired
    private ConversionService conversionService;
    
    public void convertExample() {
        // 使用ConversionService进行类型转换
        String dateStr = "2024-01-15";
        LocalDate date = conversionService.convert(dateStr, LocalDate.class);
        
        Integer number = conversionService.convert("123", Integer.class);
        Boolean flag = conversionService.convert("true", Boolean.class);
    }
}
```

---

## 三、数据格式化

### 3.1 日期格式化

```java
@RestController
@RequestMapping("/api/date")
public class DateFormatController {
    
    // 使用@DateTimeFormat注解
    @GetMapping("/format")
    public String dateFormat(
            @RequestParam @DateTimeFormat(
                pattern = "yyyy-MM-dd") Date date) {
        
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
        return "date=" + sdf.format(date);
    }
    
    // 绑定到POJO
    @PostMapping("/pojo")
    public String datePojo(@RequestBody User user) {
        // user.birthday 可以是 "2000-01-01" 格式
        return "birthday=" + user.getBirthday();
    }
}

// 在POJO上使用
public class UserDTO {
    @DateTimeFormat(pattern = "yyyy-MM-dd")
    private Date birthday;
    
    @DateTimeFormat(pattern = "yyyy-MM-dd HH:mm:ss")
    private Date createTime;
}
```

### 3.2 数字格式化

```java
public class ProductDTO {
    
    @NumberFormat(style = Style.CURRENCY)
    private BigDecimal price;  // 显示为 $99.00
    
    @NumberFormat(style = Style.PERCENT)
    private Double discount;  // 显示为 20%
    
    @NumberFormat(pattern = "#,###.##")
    private Integer stock;  // 显示为 1,000
}
```

### 3.3 全局格式化配置

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Bean
    public Formatter<LocalDate> localDateFormatter() {
        return new Formatter<LocalDate>() {
            @Override
            public LocalDate parse(String text, Locale locale) {
                return LocalDate.parse(text, 
                    DateTimeFormatter.ofPattern("yyyy-MM-dd"));
            }
            
            @Override
            public String print(LocalDate date, Locale locale) {
                return date.format(
                    DateTimeFormatter.ofPattern("yyyy-MM-dd"));
            }
        };
    }
    
    @Bean
    public Formatter<LocalDateTime> localDateTimeFormatter() {
        return new Formatter<LocalDateTime>() {
            private final DateTimeFormatter formatter = 
                DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
            
            @Override
            public LocalDateTime parse(String text, Locale locale) {
                return LocalDateTime.parse(text, formatter);
            }
            
            @Override
            public String print(LocalDateTime date, Locale locale) {
                return date.format(formatter);
            }
        };
    }
    
    @Override
    public void addFormatters(FormatterRegistry registry) {
        registry.addFormatter(localDateFormatter());
        registry.addFormatter(localDateTimeFormatter());
    }
}
```

---

## 四、Bean Validation校验

### 4.1 引入依赖

```xml
<dependencies>
    <!-- Jakarta Validation API -->
    <dependency>
        <groupId>jakarta.validation</groupId>
        <artifactId>jakarta.validation-api</artifactId>
        <version>3.0.2</version>
    </dependency>
    
    <!-- Hibernate Validator (实现) -->
    <dependency>
        <groupId>org.hibernate.validator</groupId>
        <artifactId>hibernate-validator</artifactId>
        <version>8.0.1.Final</version>
    </dependency>
    
    <!-- EL表达式支持 -->
    <dependency>
        <groupId>org.glassfish.expressly</groupId>
        <artifactId>expressly</artifactId>
        <version>5.0.0</version>
    </dependency>
</dependencies>
```

### 4.2 常用校验注解

| 注解 | 说明 | 示例 |
|------|------|------|
| `@NotNull` | 不为null | `@NotNull(message = "不能为空")` |
| `@NotBlank` | 字符串不为空且不为空串 | `@NotBlank(message = "必填")` |
| `@NotEmpty` | 集合/字符串不为空 | `@NotEmpty` |
| `@Size` | 大小在指定范围 | `@Size(min = 1, max = 100)` |
| `@Min` | 最小值 | `@Min(18)` |
| `@Max` | 最大值 | `@Max(100)` |
| `@Email` | 邮箱格式 | `@Email` |
| `@Pattern` | 正则匹配 | `@Pattern(regexp = "^1[3-9]\\d{9}$")` |
| `@Valid` | 级联校验 | `@Valid Address address` |
| `@Validated` | 分组校验 | `@Validated(CreateGroup.class)` |

### 4.3 基本校验使用

```java
// 1. 在DTO上添加校验注解
@Data
public class UserCreateRequest {
    
    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20, message = "用户名长度3-20位")
    @Pattern(regexp = "^[a-zA-Z0-9_]+$", message = "用户名只能包含字母数字下划线")
    private String username;
    
    @NotBlank(message = "密码不能为空")
    @Size(min = 6, max = 20, message = "密码长度6-20位")
    private String password;
    
    @Email(message = "邮箱格式不正确")
    private String email;
    
    @Min(value = 0, message = "年龄不能为负")
    @Max(value = 150, message = "年龄不能超过150")
    private Integer age;
    
    @Valid  // 级联校验
    private Address address;
    
    @Size(max = 5, message = "最多选择5个兴趣")
    private List<String> hobbies;
}

@Data
public class Address {
    
    @NotBlank(message = "省份不能为空")
    private String province;
    
    @NotBlank(message = "城市不能为空")
    private String city;
    
    @Size(max = 200, message = "地址不能超过200个字符")
    private String detail;
}

// 2. 在Controller中使用@Valid触发校验
@RestController
@RequestMapping("/api/users")
public class UserController {
    
    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public User createUser(
            @Valid @RequestBody UserCreateRequest request,
            BindingResult bindingResult) {
        
        // 3. 处理校验错误
        if (bindingResult.hasErrors()) {
            List<String> errors = bindingResult.getFieldErrors()
                .stream()
                .map(error -> error.getField() + ": " + error.getDefaultMessage())
                .toList();
            throw new ValidationException(errors);
        }
        
        User user = userService.create(request);
        return user;
    }
}
```

### 4.4 自定义校验注解

```java
// 1. 创建自定义校验注解
@Target({ElementType.FIELD, ElementType.PARAMETER})
@Retention(RetentionPolicy.RUNTIME)
@Constraint(validatedBy = PhoneNumberValidator.class)
public @interface PhoneNumber {
    String message() default "手机号格式不正确";
    Class<?>[] groups() default {};
    Class<? extends Payload>[] payload() default {};
}

// 2. 实现校验器
public class PhoneNumberValidator 
        implements ConstraintValidator<PhoneNumber, String> {
    
    private static final Pattern PHONE_PATTERN = 
        Pattern.compile("^1[3-9]\\d{9}$");
    
    @Override
    public void initialize(PhoneNumber constraintAnnotation) {
        // 初始化逻辑（可选）
    }
    
    @Override
    public boolean isValid(String phone, 
            ConstraintValidatorContext context) {
        if (phone == null || phone.isEmpty()) {
            return true;  // 空值由@NotNull等处理
        }
        return PHONE_PATTERN.matcher(phone).matches();
    }
}

// 3. 使用自定义注解
@Data
public class OrderCreateRequest {
    
    @PhoneNumber(message = "请输入正确的手机号")
    private String phone;
    
    @PhoneNumber
    private String emergencyContact;
}
```

### 4.5 分组校验

```java
// 1. 定义校验分组
public interface CreateGroup {}  // 创建时校验
public interface UpdateGroup {}  // 更新时校验
public interface LoginGroup {}  // 登录时校验

// 2. 在DTO上使用分组
@Data
public class UserDTO {
    
    @NotNull(groups = UpdateGroup.class, message = "更新时ID必填")
    private Long id;
    
    @NotBlank(groups = {CreateGroup.class, UpdateGroup.class})
    private String username;
    
    @NotBlank(groups = CreateGroup.class, message = "创建时密码必填")
    @Size(min = 6, groups = CreateGroup.class)
    private String password;
    
    @Email(groups = {CreateGroup.class, UpdateGroup.class})
    private String email;
    
    @NotBlank(groups = LoginGroup.class, message = "登录时用户名必填")
    private String loginUsername;
}

// 3. 在Controller中指定分组
@RestController
public class UserController {
    
    // 创建用户：使用CreateGroup分组
    @PostMapping("/users")
    public User createUser(
            @Validated(CreateGroup.class) @RequestBody UserDTO dto) {
        return userService.create(dto);
    }
    
    // 更新用户：使用UpdateGroup分组
    @PutMapping("/users/{id}")
    public User updateUser(
            @PathVariable Long id,
            @Validated(UpdateGroup.class) @RequestBody UserDTO dto) {
        return userService.update(id, dto);
    }
    
    // 登录：使用LoginGroup分组
    @PostMapping("/login")
    public TokenResponse login(
            @Validated(LoginGroup.class) @RequestBody UserDTO dto) {
        return authService.login(dto);
    }
}
```

---

## 五、文件上传

### 5.1 单个文件上传

```java
@RestController
@RequestMapping("/api/files")
public class FileUploadController {
    
    // 配置上传目录
    @Value("${upload.path:/uploads}")
    private String uploadPath;
    
    // 单个文件上传
    @PostMapping("/single")
    public UploadResult uploadFile(
            @RequestParam("file") MultipartFile file,
            @RequestParam(value = "description", required = false) String description) {
        
        // 1. 验证文件
        if (file.isEmpty()) {
            throw new RuntimeException("文件不能为空");
        }
        
        // 2. 验证文件类型
        String contentType = file.getContentType();
        if (!contentType.startsWith("image/")) {
            throw new RuntimeException("只支持图片格式");
        }
        
        // 3. 验证文件大小（在配置中限制更优）
        long maxSize = 5 * 1024 * 1024;  // 5MB
        if (file.getSize() > maxSize) {
            throw new RuntimeException("文件不能超过5MB");
        }
        
        // 4. 保存文件
        String originalFilename = file.getOriginalFilename();
        String extension = originalFilename.substring(
            originalFilename.lastIndexOf("."));
        String newFilename = UUID.randomUUID().toString() + extension;
        
        Path targetPath = Path.of(uploadPath, newFilename);
        Files.createDirectories(targetPath.getParent());
        Files.copy(file.getInputStream(), targetPath);
        
        // 5. 返回结果
        return UploadResult.builder()
            .originalFilename(originalFilename)
            .newFilename(newFilename)
            .size(file.getSize())
            .contentType(contentType)
            .url("/api/files/download/" + newFilename)
            .description(description)
            .uploadTime(LocalDateTime.now())
            .build();
    }
}
```

### 5.2 多文件上传

```java
// 多文件上传
@PostMapping("/multiple")
public List<UploadResult> uploadMultipleFiles(
        @RequestParam("files") MultipartFile[] files,
        @RequestParam(value = "directory", defaultValue = "default") String directory) {
    
    List<UploadResult> results = new ArrayList<>();
    
    for (MultipartFile file : files) {
        if (!file.isEmpty()) {
            String originalFilename = file.getOriginalFilename();
            String extension = originalFilename.substring(
                originalFilename.lastIndexOf("."));
            String newFilename = UUID.randomUUID().toString() + extension;
            
            Path targetPath = Path.of(uploadPath, directory, newFilename);
            Files.createDirectories(targetPath.getParent());
            Files.copy(file.getInputStream(), targetPath);
            
            results.add(UploadResult.builder()
                .originalFilename(originalFilename)
                .newFilename(newFilename)
                .size(file.getSize())
                .contentType(file.getContentType())
                .url("/api/files/download/" + directory + "/" + newFilename)
                .build());
        }
    }
    
    return results;
}
```

### 5.3 文件上传配置

```java
@Configuration
public class FileUploadConfig {
    
    @Bean
    public MultipartConfigElement multipartConfigElement() {
        MultipartConfigElement config = new MultipartConfigElement("");
        config.setMaxFileSize(5 * 1024 * 1024L);      // 单文件最大5MB
        config.setMaxRequestSize(50 * 1024 * 1024L); // 请求最大50MB
        config.setFileSizeThreshold(0);                // 立即写入磁盘
        return config;
    }
}
```

---

## 六、参数绑定原理

### 6.1 参数解析流程

```java
public class RequestMappingHandlerAdapter {
    
    private List<HandlerMethodArgumentResolver> argumentResolvers;
    
    @Override
    protected ModelAndView handleInternal(...) {
        // 解析方法参数
        Object[] args = getMethodArgumentValues(
            request, response, handlerMethod, webRequest, mav);
        
        // 执行方法
        Object returnValue = handlerMethod.getMethod()
            .invoke(handlerMethod.getBean(), args);
    }
    
    private Object[] getMethodArgumentValues(
            HttpServletRequest request,
            HttpServletResponse response,
            HandlerMethod handlerMethod,
            WebRequest webRequest,
            ModelAndView mav) throws Exception {
        
        MethodParameter[] parameters = handlerMethod.getMethodParameters();
        Object[] args = new Object[parameters.length];
        
        for (int i = 0; i < parameters.length; i++) {
            MethodParameter parameter = parameters[i];
            
            // 查找支持该参数的Resolver
            HandlerMethodArgumentResolver resolver = 
                findArgumentResolver(parameter);
            
            // 使用Resolver解析参数
            args[i] = resolver.resolveArgument(
                parameter, mav, webRequest, dataBinderFactory);
        }
        
        return args;
    }
}
```

### 6.2 常用参数解析器

| Resolver | 支持的参数类型 | 说明 |
|----------|---------------|------|
| `PathVariableMethodArgumentResolver` | `@PathVariable` | 路径变量解析 |
| `RequestParamMethodArgumentResolver` | `@RequestParam` | 查询参数解析 |
| `RequestBodyMethodArgumentResolver` | `@RequestBody` | 请求体解析 |
| `RequestHeaderMethodArgumentResolver` | `@RequestHeader` | 请求头解析 |
| `CookieValueMethodArgumentResolver` | `@CookieValue` | Cookie解析 |
| `ModelAttributeMethodProcessor` | `@ModelAttribute` | 模型属性解析 |
| `ServletApiArgumentResolver` | `HttpServletRequest`等 | Servlet API解析 |
| `RequestPartMethodArgumentResolver` | `@RequestPart` | 多部分请求解析 |

### 6.3 自定义参数解析器

```java
// 1. 创建自定义参数注解
@Target({ElementType.PARAMETER, ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface CurrentUser {
}

// 2. 实现自定义参数解析器
public class CurrentUserArgumentResolver 
        implements HandlerMethodArgumentResolver {
    
    @Autowired
    private UserService userService;
    
    @Override
    public boolean supportsParameter(MethodParameter parameter) {
        // 支持@CurrentUser注解的User类型参数
        return parameter.hasParameterAnnotation(CurrentUser.class) &&
               parameter.getParameterType().equals(User.class);
    }
    
    @Override
    public Object resolveArgument(
            MethodParameter parameter,
            ModelAndViewContainer mavContainer,
            NativeWebRequest webRequest,
            WebDataBinderFactory binderFactory) 
            throws Exception {
        
        // 从SecurityContext获取当前用户ID
        Long userId = SecurityContextHolder.getCurrentUserId();
        
        // 查询用户信息
        return userService.findById(userId);
    }
}

// 3. 注册解析器
@Configuration
public class WebConfig implements WebMvcConfigurer {
    
    @Autowired
    private CurrentUserArgumentResolver currentUserResolver;
    
    @Override
    public void addArgumentResolvers(
            List<HandlerMethodArgumentResolver> resolvers) {
        resolvers.add(currentUserResolver);
    }
}

// 4. 使用自定义参数
@RestController
@RequestMapping("/api/profile")
public class ProfileController {
    
    @GetMapping
    public User getCurrentUser(@CurrentUser User currentUser) {
        // 直接获取当前登录用户
        return currentUser;
    }
    
    @PutMapping
    public User updateProfile(
            @CurrentUser User currentUser,
            @RequestBody UpdateProfileRequest request) {
        return userService.update(currentUser.getId(), request);
    }
}
```

---

## 七、小结

SpringMVC的参数绑定与数据校验是构建健壮Web应用的关键：

1. **参数绑定**：支持基本类型、POJO、数组、列表等多种类型的自动绑定
2. **类型转换**：内置大量转换器，支持自定义Converter和Formatter
3. **数据格式化**：支持日期、数字等的格式化注解
4. **数据校验**：基于Bean Validation规范，支持分组校验和自定义校验
5. **文件上传**：支持单文件、多文件上传及类型校验
6. **扩展机制**：支持自定义参数解析器、类型转换器

掌握这些特性，能够帮助我们：
- 简化控制器代码
- 保证数据的合法性
- 构建安全、可靠的API接口
