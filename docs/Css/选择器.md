# 选择器

## 基本选择器

### 通配选择器 *
```css
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box; /* 推荐全局设置 */
}
```

### 元素选择器 标签名
```css
p {
  color: #333;
  line-height: 1.6;
}

div {
  margin-bottom: 20px;
}

button {
  cursor: pointer;
}
```

### 类选择器 .className
```css
/* 常用：样式复用 */
.button {
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
}

.primary {
  background-color: #007bff;
  color: white;
}

.danger {
  background-color: #dc3545;
  color: white;
}

/* 多类选择器 */
.btn.btn-large.active {
  /* 同时有 btn、btn-large、active 三个类 */
}
```

### ID选择器 #idName
```css
#header {
  background-color: #f8f9fa;
  padding: 20px;
}

#main-content {
  max-width: 1200px;
  margin: 0 auto;
}

#footer {
  margin-top: 50px;
  text-align: center;
}
```

### 属性选择器 [attribute]
```css
/* 基础属性选择器 */
[disabled] {
  opacity: 0.5;
  cursor: not-allowed;
}

/* 精确匹配 */
[type="text"] {
  border: 1px solid #ccc;
}

[href="https://example.com"] {
  color: green;
}

/* 开始匹配 ^= */
a[href^="https"] {
  /* 所有https链接 */
  color: green;
}

[class^="icon-"] {
  /* 所有以icon-开头的类 */
  background-image: url("icons.png");
}

/* 结束匹配 $= */
a[href$=".pdf"]::after {
  content: " 📄";
  font-size: 0.8em;
}

[src$=".jpg"] {
  border: 2px solid #ddd;
}

/* 包含匹配 *= */
[class*="button"] {
  /* 类名包含button */
  cursor: pointer;
}

[href*="google"] {
  /* href包含google */
  color: #4285f4;
}

/* 单词匹配 ~= */
[class~="active"] {
  /* 类名中有独立的active单词 */
  font-weight: bold;
}

/* 连字符匹配 |= */
[lang|="en"] {
  /* lang属性以en开头或en-开头 */
  font-family: "Arial", sans-serif;
}
```

## 组合选择器（连接多个选择器）

### 后代选择器 空格
```css
/* 选择article内的所有p元素（无论嵌套多深） */
article p {
  margin-bottom: 1em;
}

/* 选择nav中的所有li */
nav ul li {
  display: inline-block;
}

/* 选择.card内的所有img */
.card img {
  width: 100%;
  height: auto;
}
```

### 子元素选择器 >
```css
/* 只选择直接子元素 */
nav > ul {
  /* nav的直接子元素ul */
  list-style: none;
}

ul > li {
  /* ul的直接子元素li */
  padding: 10px 0;
}

.container > .row {
  /* container的直接子元素row */
  display: flex;
}

/* 多层子选择器 */
header > nav > ul > li > a {
  color: white;
}
```

### 相邻兄弟选择器 +
```css
/* 选择紧跟在h2后面的p元素 */
h2 + p {
  margin-top: 0;
  font-style: italic;
}

/* 选择紧跟在.alert后面的.btn */
.alert + .btn {
  margin-top: 10px;
}

/* 选择紧跟在label后面的input */
label + input {
  margin-left: 10px;
}
```

### 通用兄弟选择器 ~
```css
/* 选择h2后面所有的同级p元素 */
h2 ~ p {
  color: #666;
}

/* 选择.active后面的所有同级.item */
.active ~ .item {
  opacity: 0.5;
}

/* 选择第一个checkbox后面所有的checkbox */
input[type="checkbox"]:first-of-type ~ input[type="checkbox"] {
  margin-left: 15px;
}
```

## 伪类选择器（元素的状态或位置）

### 链接状态伪类
```css
/* 未访问的链接 */
a:link {
  color: #007bff;
  text-decoration: none;
}

/* 已访问的链接 */
a:visited {
  color: #6f42c1;
}

/* 鼠标悬停 */
a:hover {
  text-decoration: underline;
  color: #0056b3;
}

/* 鼠标点击时（激活状态） */
a:active {
  color: #dc3545;
}
```

### 用户界面伪类
```css
/* 获得焦点的元素 */
input:focus {
  outline: 2px solid #80bdff;
  border-color: #007bff;
}

/* 选中的复选框或单选按钮 */
input:checked {
  background-color: #007bff;
}

/* 禁用的元素 */
input:disabled {
  background-color: #e9ecef;
  cursor: not-allowed;
}

/* 启用的元素 */
input:enabled {
  background-color: white;
}

/* 必填字段 */
input:required {
  border-left: 3px solid #dc3545;
}

/* 有效/无效的表单字段 */
input:valid {
  border-color: #28a745;
}

input:invalid {
  border-color: #dc3545;
}

/* 范围选择器 */
input:in-range {
  color: green;
}

input:out-of-range {
  color: red;
}
```

### 结构伪类（重要！）
```css
/* 第一个子元素 */
ul li:first-child {
  border-top-left-radius: 5px;
  border-top-right-radius: 5px;
}

/* 最后一个子元素 */
ul li:last-child {
  border-bottom-left-radius: 5px;
  border-bottom-right-radius: 5px;
}

/* 第n个子元素 */
ul li:nth-child(3) {
  /* 第三个li */
  background-color: #f0f0f0;
}

tr:nth-child(2n) {
  /* 偶数行（2n等价于even） */
  background-color: #f8f9fa;
}

tr:nth-child(2n+1) {
  /* 奇数行（2n+1等价于odd） */
  background-color: white;
}

/* 从后往前数的第n个子元素 */
ul li:nth-last-child(1) {
  /* 倒数第一个 */
  border-bottom: none;
}

/* 唯一子元素 */
div:only-child {
  width: 100%;
}

/* 第一个/最后一个某种类型 */
p:first-of-type {
  margin-top: 0;
}

p:last-of-type {
  margin-bottom: 0;
}

/* 第n个某种类型 */
article:nth-of-type(2n) {
  background-color: #f9f9f9;
}

/* 唯一某种类型 */
img:only-of-type {
  max-width: 100%;
}

/* 空元素 */
div:empty {
  display: none;
}
```

### 其他功能伪类
```css
/* 否定伪类：选择不匹配的元素 */
input:not([type="submit"]) {
  border: 1px solid #ccc;
}

div:not(.hidden) {
  display: block;
}

/* 目标伪类：URL片段标识符对应的元素 */
:target {
  background-color: yellow;
}

/* 根元素 */
:root {
  /* 等价于 html 选择器，但优先级更高 */
  --primary-color: #007bff;
}

/* 全屏伪类 */
video:fullscreen {
  width: 100%;
  height: 100%;
}

/* 焦点可见伪类（仅键盘操作时显示焦点） */
button:focus-visible {
  outline: 2px solid blue;
}

/* 占位文本 */
input::placeholder {
  color: #999;
}
```